# US-002 — Make collector game-neutral and add LoL services

> **For the implementer (`feature-json-implement-step`):** implement this story only. Do **not** deploy. Do **not** run `docker compose up/down/restart/recreate` against `/root/work/polymarket-collector`. Do **not** touch `poly-maker`. Do **not** restart `/root/work/dota_2_model` or live-paper. Do **not** reboot the VPS. Do **not** start US-003. Work on `main`.

**Story:** `current-task/feature.json` → `US-002` (`passes: false`).
**Repo:** `/root/work/polymarket-collector` (sibling of `betting_workspace`).
**FR:** FR-2 (one binary, required `POLYMARKET_TAG_ID`, separate archive roots).
**Commit (collector):** `Make the collector tag id game-neutral and add the LoL pair of services.`

---

## 0. Constraints (hard)

- Git only inside `polymarket-collector` (code) and `betting_workspace` (this plan is already written; implementer updates `feature.json` + `progress.txt` after the work).
- `../poly-maker` is frozen. Do not edit it.
- Do not `docker compose restart|up|down|create` anything under `dota_2_model` or live-paper.
- US-002 is **code + compose.yaml + tests**. Applying compose to the running VPS collector is **US-003**.
- Production collector is currently running as services `archive` and `compact` (containers `polymarket-collector-archive-1` / `polymarket-collector-compact-1`, Up ~8 days). Editing `compose.yaml` on disk does not recreate them. `docker compose up` after the rename **would**. Never apply it here.
- Do not `mkdir` `/var/lib/polymarket-lol-archive` (US-003).
- Do not commit `.env`.

---

## 1. Read first (verified from the current tree)

Read these before editing. They are the source of truth, not this section's paraphrase.

| Path | Why |
|---|---|
| `/root/work/polymarket-collector/AGENTS.md` | Yarn Berry, `yarn check`, no barrels |
| `/root/work/polymarket-collector/docs/learnings.md` | Compose needs a present `.env`; collect/compact split |
| `/root/work/polymarket-collector/src/config.ts` | `dotaTagId` / `DOTA_TAG_ID` default `"102366"` |
| `/root/work/polymarket-collector/src/config.test.ts` | Defaults, empty-tag-as-default, `SettingsLive` |
| `/root/work/polymarket-collector/src/app.ts` | **Four** `dotaTagId` sites (live-lol plan said three) |
| `/root/work/polymarket-collector/src/app.test.ts` | `withArchiveRoot` env is `ARCHIVE_ROOT` only |
| `/root/work/polymarket-collector/src/discovery.ts` ~L205 | `invalid Dota tag id` |
| `/root/work/polymarket-collector/src/schema/primitives.ts` L38 | TagId docstring still names `DOTA_TAG_ID` default |
| `/root/work/polymarket-collector/compose.yaml` | services `archive` / `compact`, `DOTA_TAG_ID` |
| `/root/work/polymarket-collector/src/docker.test.ts` | String-asserts compose service names, env, bind count |
| `/root/work/betting_workspace/current-task/feature.json` | US-002 `changes[]` is the acceptance list |
| `/root/work/betting_workspace/docs/live-lol/2026-08-29-live-lol-implementation-plan.md` § Stage 1 | Same rename + LoL pair |

Do **not** treat `docs/completed-tasks/effect-polymarket-archive-daemon/**` as something to update. Historical.

---

## 2. Current names → target names

### Config / code

| Current | Target |
|---|---|
| `Settings.dotaTagId` | `Settings.polymarketTagId` |
| `RawSettings.dotaTagId` | `RawSettings.polymarketTagId` |
| env `DOTA_TAG_ID` | env `POLYMARKET_TAG_ID` |
| `Config.string("DOTA_TAG_ID").pipe(Config.withDefault("102366"))` | `Config.string("POLYMARKET_TAG_ID")` — **no default**, same shape as `ARCHIVE_ROOT` |
| log keys `dotaTagId:` | `polymarketTagId:` |
| discovery error `` `invalid Dota tag id: ${tagId}` `` | `` `invalid Polymarket tag id: ${tagId}` `` |
| TagId docstring `` Gamma tag ID (string; `DOTA_TAG_ID` default `"102366"`). `` | Game-neutral, no default. Example: `` Gamma tag ID (string; required `POLYMARKET_TAG_ID`, no default). `` |

Numeric Dota tag `102366` and LoL tag `65` stay as **compose env values**, not code defaults. Classification (`moneyline` / `child_moneyline`) does not change. `openDiscovery(store, tagId, interval)` still takes a string; only the Settings field and error text change.

### Compose services

| Current | Target | `PROCESS_ROLE` | `POLYMARKET_TAG_ID` | bind `source` | `build: .` | `oom_score_adj: -500` |
|---|---|---|---|---|---|---|
| `archive` | `archive-dota` | `collect` | `102366` | `/var/lib/polymarket-dota-archive` | **yes (only builder)** | yes |
| `compact` | `compact-dota` | `compact` | `102366` | `/var/lib/polymarket-dota-archive` | no | no |
| — | `archive-lol` | `collect` | `65` | `/var/lib/polymarket-lol-archive` | no | yes |
| — | `compact-lol` | `compact` | `65` | `/var/lib/polymarket-lol-archive` | no | no |

All four keep: `image: polymarket-collector:latest`, `init: true`, `restart: unless-stopped`, `stop_grace_period: 30s`, `env_file: .env`, `ARCHIVE_ROOT: /data`, interval/disk env, bind `target: /data`, `logging.driver: local`. Copy the existing Dota pair; do not invent YAML anchors.

Keep **exactly one** `build: .` (on `archive-dota`). `archive-lol` uses the same image, like compact. `docker.test.ts` currently requires `build: .` length 1 — keep that.

---

## 3. File-level changes

### 3.1 Edit `src/config.ts`

Rename the field on `Settings`, `RawSettings`, `rawConfig`, and `Settings.of`.

Remove `Config.withDefault("102366")`. Tag load must fail when the env key is absent, same as `ARCHIVE_ROOT`.

In `validateSettings`, reject empty and whitespace-only:

```ts
if (raw.polymarketTagId.trim() === "")
  return yield* new ConfigurationError({
    message: `POLYMARKET_TAG_ID must be a non-empty string, got ${JSON.stringify(raw.polymarketTagId)}`
  })
```

Place it with the other semantic checks (next to `processRole` is fine). Do **not** add numeric/safe-integer validation here — `openDiscovery` already rejects non-positive parse. Story asks for missing/empty/whitespace only.

"Missing" at `validateSettings` is the empty/whitespace case (`RawSettings` always has a string). Missing **env** is `loadSettings` / `Config.string` with no default.

### 3.2 Edit `src/app.ts` (four sites, not three)

Verified current lines:

- L89 — `collectMain` startup `annotateLogs({ dotaTagId: settings.dotaTagId, ... })`
- L153 — `openMetadataStore(..., settings.dotaTagId)`
- L157 — `openDiscovery(..., settings.dotaTagId, ...)`
- L163 — `discovery ready` `annotateLogs({ dotaTagId: settings.dotaTagId, ... })`

Rename field and log key to `polymarketTagId` at all four. `compactMain` does not read the tag; no change there. Compact still **loads** Settings, so compose must still pass `POLYMARKET_TAG_ID` to compact services or they fail at boot.

### 3.3 Edit `src/discovery.ts`

L205: `invalid Dota tag id` → `invalid Polymarket tag id`. No other discovery logic. No new discovery test required (none asserts this string today).

### 3.4 Edit `src/schema/primitives.ts`

L38 docstring only. `TagId` brand stays a string. `primitives.test.ts` already accepts `"102366"`; leave it.

### 3.5 Edit `compose.yaml`

1. Rename `archive:` → `archive-dota:`, `compact:` → `compact-dota:`.
2. Rename env `DOTA_TAG_ID: "102366"` → `POLYMARKET_TAG_ID: "102366"` on the Dota pair.
3. Append `archive-lol` / `compact-lol` as copies of the Dota collect/compact pair with `POLYMARKET_TAG_ID: "65"` and bind source `/var/lib/polymarket-lol-archive`.
4. `archive-lol` gets `oom_score_adj: -500` and `PROCESS_ROLE: "collect"`. No `build: .`.
5. `compact-lol` matches `compact-dota` (no oom, no build).

Suggested order: `archive-dota`, `compact-dota`, `archive-lol`, `compact-lol`.

### 3.6 Tests to update

#### `src/config.test.ts` (required by the story)

`validRaw.dotaTagId` → `polymarketTagId: "102366"`. All `settings.dotaTagId` assertions → `polymarketTagId`. All env `DOTA_TAG_ID` → `POLYMARKET_TAG_ID`.

Invert / replace these cases:

| Current test | After |
|---|---|
| `"applies defaults when only ARCHIVE_ROOT is set"` (expects tag `102366`) | Rename to something like `"applies remaining defaults when ARCHIVE_ROOT and POLYMARKET_TAG_ID are set"`. Env **must** include `POLYMARKET_TAG_ID` (use `"102366"` or `"65"`). Still assert interval 60, warn 10, stop 5, role `collect`. Do **not** invent a tag default. |
| `"treats an empty DOTA_TAG_ID as absent and applies the default"` | Delete. Replace with reject-empty (below). |
| `"layer build succeeds with valid env"` (`ARCHIVE_ROOT` only) | Provide `POLYMARKET_TAG_ID` too. |
| `"uses overridden values verbatim"` | `POLYMARKET_TAG_ID: "777"` → `settings.polymarketTagId === "777"`. |
| Other `loadSettings` cases that only set `ARCHIVE_ROOT` | Add `POLYMARKET_TAG_ID` so they still test interval/disk/role, not the new required tag. |

**Add** (story: missing, empty, valid):

1. **`validateSettings` rejects empty** — `{ ...validRaw, polymarketTagId: "" }` → `ConfigurationError`, message contains `POLYMARKET_TAG_ID`.
2. **`validateSettings` rejects whitespace-only** — `"   "` / `"\t"` → same. Empty env never reaches `validateSettings` (Effect env provider treats `""` as absent), so whitespace is the check that actually hits `trim()`.
3. **`validateSettings` accepts a valid tag** — `"102366"` and one other non-default such as `"65"` both succeed (`polymarketTagId` echoed). Proves no Dota default in validation.
4. **`loadSettings` rejects missing `POLYMARKET_TAG_ID`** — env `{ ARCHIVE_ROOT: "/data" }` → `ConfigurationError`, message contains `POLYMARKET_TAG_ID` (and typically `Failed to load configuration`).
5. **`loadSettings` rejects empty `POLYMARKET_TAG_ID`** — env `{ ARCHIVE_ROOT: "/data", POLYMARKET_TAG_ID: "" }` → `ConfigurationError` containing `POLYMARKET_TAG_ID`. Today this succeeds via default; after the change it must fail. Do not assert `validateSettings` wording here — empty env is usually a Config missing-key, not the trim check.
6. **`loadSettings` accepts a valid tag** — `POLYMARKET_TAG_ID: "65"` with `ARCHIVE_ROOT` → `settings.polymarketTagId === "65"`.

Keep existing PROCESS_ROLE / disk / interval tests; just add the required tag to their env.

#### `src/app.test.ts` (required or `main` tests die)

`withArchiveRoot` currently:

```ts
ConfigProvider.fromEnv({ env: { ARCHIVE_ROOT: root } })
```

Every `AppLive` / `main` / `collectMain` / `compactMain` test uses this. After the default is gone they will fail with missing `POLYMARKET_TAG_ID`.

Change to:

```ts
ConfigProvider.fromEnv({ env: { ARCHIVE_ROOT: root, POLYMARKET_TAG_ID: "102366" } })
```

Use `102366` so daemon tests still match Dota gamma fixtures. Do not rewrite fixtures.

`"fails with ConfigurationError when ARCHIVE_ROOT is absent"` uses `env: {}`. `Config.all` may then fail on `POLYMARKET_TAG_ID` first and break `toContain("ARCHIVE_ROOT")`. Fix by providing `POLYMARKET_TAG_ID` in that test's env so the only missing required key is `ARCHIVE_ROOT`. Missing-tag coverage lives in `config.test.ts`.

Other `withArchiveRoot` helpers (`journal.test.ts`, `discovery.test.ts`, `parquet.testing.ts`, …) are temp directories only; they pass `"102366"` as a function argument, not via Settings. Leave them.

#### `src/docker.test.ts` (required or compose tests die)

Rewrite the compose assertions. Do **not** update `docs/docker-compose.md` (US-014 / operator docs). The runbook test (`"documents the only operator start and log commands"`) still reads the old runbook and must keep expecting `archive` / `compact` **in the runbook**, not in compose.

Update:

- `"keeps archive and compact services with the contract settings"` — four services; `POLYMARKET_TAG_ID` not `DOTA_TAG_ID`; Dota pair `102366` + `/var/lib/polymarket-dota-archive`; LoL pair `65` + `/var/lib/polymarket-lol-archive`; `init` / `restart` / `stop_grace_period` count **4**; `oom_score_adj: -500` count **2** (both collect services); `build: .` still **1**; `PROCESS_ROLE` collect on both archives, compact on both compacts. The old `slice(indexOf("  archive:"), indexOf("  compact:"))` **breaks** with four services — extract per-service blocks by name instead.
- `"keeps the Compose YAML vocabulary explicit"` — count `archive-dota:`, `compact-dota:`, `archive-lol:`, `compact-lol:` (each 1). Bind lines `- type: bind` count **4**. Do not still require bare `archive:` / `compact:`.

Leave Dockerfile / dockerignore tests unchanged.

### 3.7 Do not edit (this story)

- `docs/docker-compose.md`, `docs/polymarket_dota_archive_contracts.md`, `docs/polymarket_dota_archive_plan.md`, `README.md`, collector `AGENTS.md`, `docs/learnings.md` — stale `DOTA_TAG_ID` / `archive`+`compact` left for US-014 (workspace learnings) / later operator-doc work. docker.test.ts runbook half stays green.
- `docs/completed-tasks/**`
- `.env` / `.env.example` (Telegram only; tag is compose `environment:`)
- `compose.monitoring.yaml`
- `../dota_2_model` (including `src/collect/01_build_universe.py` `DOTA_TAG_ID = 102366`)
- `../poly-maker`
- Host `/var/lib/polymarket-lol-archive`

---

## 4. Implementation order

1. `config.ts` rename + drop default + `validateSettings` trim check.
2. `app.ts` four sites.
3. `discovery.ts` error string; `primitives.ts` docstring.
4. `config.test.ts` + `app.test.ts` env fixture (do these before running tests or almost everything fails).
5. `compose.yaml` four services.
6. `docker.test.ts` compose assertions.
7. Verify with commands below. Do **not** apply compose.
8. Collector commit. Then betting_workspace `feature.json` / `progress.txt` commit.

---

## 5. Commands

All from `/root/work/polymarket-collector` unless noted. `.env` already exists (required by `env_file:`). Do not create or chown the LoL archive dir.

```bash
# After code+test edits:
yarn test src/config.test.ts src/docker.test.ts src/app.test.ts
yarn typecheck
yarn check
```

`yarn check` = typecheck + Effect diagnostics + oxlint `src` + full vitest. That is the green bar. Story also names typecheck separately; `yarn typecheck` is `tsc -p tsconfig.json --noEmit`.

**Compose dry parse only** — never `up` / `down` / `restart` / `create` / `--force-recreate` / `--remove-orphans`:

```bash
docker compose config --services
docker compose config
```

`--services` must print exactly (order may vary):

```
archive-dota
compact-dota
archive-lol
compact-lol
```

From `docker compose config` (interpolated YAML/JSON), assert:

- No service named `archive` or `compact`.
- `archive-dota` and `compact-dota`: `POLYMARKET_TAG_ID=102366`, bind source `/var/lib/polymarket-dota-archive`, target `/data`.
- `archive-lol` and `compact-lol`: `POLYMARKET_TAG_ID=65`, bind source `/var/lib/polymarket-lol-archive`, target `/data`.
- Four services, one shared image `polymarket-collector:latest`.
- Only `archive-dota` has a build section.

If `docker compose config` warns that `/var/lib/polymarket-lol-archive` does not exist, ignore it. Do not create the directory. Do not start the LoL pair.

Confirm running production is still the **old** names (untouched):

```bash
docker compose ps
```

Expect `archive` and `compact` still Up. If they are not, stop and report — do not "fix" by recreating.

---

## 6. Commit strategy

Work on `main` in both repos. Do not commit broken code. Do not `--amend` unless the implement-step hook rules apply.

### 6.1 `polymarket-collector` — one focused commit

Stage only the files listed in §3. Message (from the live-lol plan):

```
Make the collector tag id game-neutral and add the LoL pair of services.
```

Do not include `.env`, `docs/completed-tasks/**`, or dota_2_model.

### 6.2 `betting_workspace` — after collector is green

Per `feature-json-implement-step`:

1. Set `userStories[id=US-002].passes` to `true`. Do not flip US-003 or later.
2. **Append** (never replace) `current-task/progress.txt` with:

```
## [Date/Time] - US-002

- What was implemented
- **Learnings for future iterations:**
  - ...
```

Useful learnings to record: Effect empty env ≡ missing (so empty `POLYMARKET_TAG_ID` fails at Config load, whitespace hits `validateSettings`); `app.test.ts` `withArchiveRoot` must supply the tag; production still runs `archive`/`compact` until US-003; docker.test.ts runbook half still documents old names.

3. Commit those two files in `betting_workspace` only. Suggested: `feat: US-002 - Сделать collector game-neutral и добавить LoL services`.

Do not push unless the user asks.

---

## 7. Risks

| Risk | What to do |
|---|---|
| **Compose service rename vs running production.** Live stack is `archive` + `compact`. A `docker compose up -d` after this commit starts **new** `archive-dota`/`compact-dota` (and would try LoL binds). `--remove-orphans` would **stop the running Dota collector**. US-002 must **not** apply compose. Only `docker compose config` + `ps`. Deploy is US-003. |
| Editing `compose.yaml` on the VPS checkout is fine; Engine does not reload it until an apply. | Do not apply. |
| `restart: unless-stopped` on a crash/reboot would start **old** image+old compose until US-003. | Out of scope. Do not reboot. |
| `app.test.ts` env fixture forgotten | Almost all daemon tests fail missing `POLYMARKET_TAG_ID`. Fix `withArchiveRoot` first. |
| Empty-string Config semantics | `POLYMARKET_TAG_ID=""` is absent, not `validateSettings("")`. Cover both layers. |
| `docker.test.ts` two-block slice | Must rewrite; do not keep `indexOf("  archive:")`. |
| Stale operator docs | Leave them. Updating runbook without US-014 will fail the runbook docker test or expand scope. |
| Bind source missing for LoL | `config` may warn; `up` would be US-003 after `mkdir`/`chown`. |

---

## 8. Acceptance checklist (from `feature.json` US-002 `changes[]`)

Copy. Implementer ticks against the diff, not memory.

- [ ] In `../polymarket-collector/src/config.ts` rename `dotaTagId` → `polymarketTagId` and `DOTA_TAG_ID` → `POLYMARKET_TAG_ID` with **no default value**
- [ ] `validateSettings` rejects missing, empty, or whitespace-only `POLYMARKET_TAG_ID`
- [ ] Update tag usages in `src/app.ts`, error text in `src/discovery.ts`, and description in `src/schema/primitives.ts` to game-neutral wording
- [ ] In `compose.yaml` rename `archive`/`compact` → `archive-dota`/`compact-dota` and pass `POLYMARKET_TAG_ID=102366`
- [ ] In `compose.yaml` add `archive-lol`/`compact-lol` with `POLYMARKET_TAG_ID=65` and bind mount `/var/lib/polymarket-lol-archive`
- [ ] `docker compose config` shows four services, correct tag ids, separate archive root per pair
- [ ] Add config tests for missing, empty, and valid `POLYMARKET_TAG_ID`
- [ ] Add or update automated tests
- [ ] Tests pass
- [ ] `yarn check` passes
- [ ] Typecheck passes

Plus implementer-owned (skill, not in `changes[]` but required to leave a green tree):

- [ ] `src/app.test.ts` `withArchiveRoot` provides `POLYMARKET_TAG_ID`
- [ ] `src/docker.test.ts` compose assertions match the four services
- [ ] No `docker compose up/down/restart/recreate`; running services still named `archive` and `compact`
- [ ] `US-002.passes: true`; progress appended; one collector commit + workspace progress commit
- [ ] US-003 not started
