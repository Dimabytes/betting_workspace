# US-014 — Update operator docs and VPS diagnostics for two games

> **For the implementer (`feature-json-implement-step`):** docs and diagnostic script only. Do **not** `docker compose up|down|restart|recreate|start|stop` (or `--force-recreate`) anything under `dota_2_model`. Do **not** edit `.env`. Do **not** start US-015. Do **not** touch `poly-maker`. Do **not** edit `../polymarket-collector` product files (learnings live in betting_workspace). Work on `main`. FILE-ONLY.

**Story:** `current-task/feature.json` → `US-014` (`passes: false`).
**Repos:** `/root/work/dota_2_model` (AGENTS.md, docs/live-paper.md) **and** `/root/work/betting_workspace` (learnings, vps-live-paper skill, this plan, feature.json, progress).
**FR:** FR-10 runbook half (exact paper-deploy / no-live-LoL-orders / rollback commands). Execution of those commands is US-015.
**Commits:**
- dota_2_model: `feat: US-014 - Обновить operator docs и VPS diagnostics под две игры`
- betting_workspace: same title after dota_2_model is green

---

## 0. Constraints (hard)

- Git only inside `dota_2_model` (the two markdown files) and `betting_workspace` (learnings, skill, feature.json, progress, this plan).
- `../poly-maker` is frozen. Do not edit it.
- Do **not** apply compose. Running trader is still `live-paper` (Id `a188e0d8ebb5…`, StartedAt `2026-08-30T07:23:39Z`, Cmd **without** `--mode`, bind `./data/live_paper`). YAML already names `trader-live` / `trader-paper`. A stray `up -d` would start those on **empty** `live_paper_live` / `live_paper_paper` while production wallet stays in `./data/live_paper`. Stop and report.
- Do **not** edit `.env`. Host `.env` still has `LIVE_TRADING=1` and neither per-game mode. US-015 owns that edit.
- Do **not** recreate collectors. They are already `archive-dota` / `compact-dota` / `archive-lol` / `compact-lol`.
- Docs match **code** (`compose.yaml`, CLI, env names, paths), not `docs/live-lol/2026-08-29-live-lol-implementation-plan.md` if they drifted, and not the still-running `live-paper` container name as if it were the compose truth.
- Python package stays `live_paper`. CLI `prog` stays `live-paper`. Log / Telegram prefix stays `live-paper session started:`. Do not "fix" those in code.
- `make lint-all` skips untracked files: `git add` edited markdown before lint in dota_2_model.
- Preserve any uncommitted Kalshi dirt. Re-check `git status` before staging. Stage only the files this plan names.

---

## 1. Read first (verified from the current tree)

Read these before editing. They are the source of truth, not this section's paraphrase.

| Path | Why |
|---|---|
| `/root/work/dota_2_model/compose.yaml` | Services `trader-live` (`--mode live`) and `trader-paper` (`--mode paper`). No `live-paper` service, no `env_file`, no `ARCHIVE_ROOT`, no `LIVE_TRADING`. Archives `/archive/dota` and `/archive/lol`. State `./data/live_paper_live` vs `./data/live_paper_paper` both target `/app/data/live_paper`. Models `./data/new_model:ro` and `./data/lol/models:ro`. PK/BROWSER_ADDRESS only on live. Mode/archive env on **both**. |
| `/root/work/dota_2_model/Makefile` L6, L65–66 | Target `trader`, `MODE ?= paper`, recipe `$(PY) -m live_paper.orchestrator daemon --mode $(MODE)`. No `live-paper:` target. |
| `/root/work/dota_2_model/src/live_paper/orchestrator.py` L22–28 | `prog="live-paper"`, required `--mode` `live\|paper`. |
| `/root/work/dota_2_model/src/live_paper/trading_mode.py` | `execution_mode` is CLI only. `assigned_games` reads `DOTA_TRADING_MODE` / `LOL_TRADING_MODE` (`off`/`paper`/`live`; missing/empty/unknown → `TradingDisabled`). `reject_legacy_env` fails if `LIVE_TRADING` is set at all. |
| `/root/work/dota_2_model/src/live_paper/game_profile.py` | Frozen `GAME_PROFILES`. Dota: `DOTA_ARCHIVE_ROOT`, `DOTA_TRADING_MODE`, RADIANT/DIRE, `dota-map`, `data/new_model/production`, Steam, cap 350. LoL: `LOL_ARCHIVE_ROOT`, `LOL_TRADING_MODE`, BLUE/RED, `lol-map`, `data/lol/models/production`, no Steam, cap `None`. |
| `/root/work/dota_2_model/src/live_paper/host_resources.py` L142–155 | Boot: reject legacy → `assigned_games` → empty assignment logs `live-paper idle: mode=%s assigned=()` and waits. Else `live-paper assigned: mode=%s games=%s`. |
| `/root/work/dota_2_model/src/live_paper/collector_sidecars.py` L91–96 | `load_archive_root(profile)` reads `profile.archive_root_env`. No `ARCHIVE_ROOT` fallback. |
| `/root/work/dota_2_model/src/live_paper/model_server.py` L168–171 | `load_production_model(profile)` from `profile.production_model_dir` / `model.txt`+`model.json`. Alert prefix `live-paper model startup blocked:`. Contract is shared 12 `FEATURE_COLUMNS`. |
| `/root/work/dota_2_model/src/live_paper/feed_selection.py` L17–73 | LoL (`uses_steam=False`) is GRID-only. Dota still delay-compares Steam vs GRID. |
| `/root/work/dota_2_model/src/live_paper/discovery.py` L169–201, L524–527 | Cycle log includes `game=` and `archive=`. LoL uses sidecar `grid_series_id` only (no Match Winner inheritance). |
| `/root/work/dota_2_model/src/live_paper/session_config.py` L17 | Template is `config/trading.toml`. Extra profile tables ignored; missing still errors. Risk dollars from sum of loaded clips. |
| `/root/work/dota_2_model/config/trading.toml` | `[profiles.dota-map]` clip 50, `[profiles.lol-map]` clip 20, `[kalshi]`. |
| `/root/work/dota_2_model/src/live_paper/match_meta.py` L86–87, L572–574 | Writes schema 5 with `game`. Reads 3/4/5. Missing `game` → `dota`. |
| `/root/work/dota_2_model/src/shared/constants/paths.py` L57–70 | In-container path is still `data/live_paper` (`LIVE_PAPER_DIR`). Host isolation is the compose bind, not a Python rename. |
| `/root/work/polymarket-collector/compose.yaml` | Four services. Tag `102366` on dota pair, `65` on lol pair. Host `/var/lib/polymarket-dota-archive` and `/var/lib/polymarket-lol-archive`. In-container `ARCHIVE_ROOT=/data`. Required `POLYMARKET_TAG_ID`, no default. |
| `/root/work/dota_2_model/tests/test_trader_compose.py` | Compose/Make assertions. Do not change unless you change compose (you must not). |
| `/root/work/betting_workspace/.shared-skills/vps-live-paper/SKILL.md` | Operator skill. Four hardlinks of the same inode (`.shared-skills`, `.cursor`, `.claude`, `.agents`). Edit one file. |
| `current-task/feature.json` US-014 `changes[]` | Acceptance list. |
| `current-task/progress.txt` | US-008 rename is file-only; US-003 already applied collectors; US-015 is the recreate. |

Same-inode skill copies (do not duplicate edits):

```
.shared-skills/vps-live-paper/{SKILL.md,log-map.md,scripts/summarize.py}
.cursor/skills/vps-live-paper/…
.claude/skills/vps-live-paper/…
.agents/skills/vps-live-paper/…
```

`ls -i` before and after. If inodes diverge, you copied instead of writing the hardlink.

---

## 2. Current vs target

### 2.1 Names that are true in code (write these)

| Role | Exact name |
|---|---|
| Compose traders | `trader-live`, `trader-paper` |
| Compose collectors | `archive-dota`, `compact-dota`, `archive-lol`, `compact-lol` |
| Makefile | `make trader` (`MODE=paper` default) or `make trader MODE=live` |
| Process CLI | `python -m live_paper.orchestrator daemon --mode live\|paper` |
| Process mode | CLI `--mode` only. Not `LIVE_TRADING`. |
| Game assignment | `DOTA_TRADING_MODE`, `LOL_TRADING_MODE` ∈ `off` \| `paper` \| `live`. Missing/empty/unknown is a startup error. `off` is the only way to unassign. |
| Archive env (trader) | `DOTA_ARCHIVE_ROOT=/archive/dota`, `LOL_ARCHIVE_ROOT=/archive/lol` (compose literals) |
| Host archives | `/var/lib/polymarket-dota-archive`, `/var/lib/polymarket-lol-archive` |
| Host trader state | `./data/live_paper_live` (live process), `./data/live_paper_paper` (paper process) |
| In-container trader state | `/app/data/live_paper` (both; isolation is the host bind) |
| Dota model | `data/new_model/production/` (`model.txt` + `model.json`) |
| LoL model | `data/lol/models/production/` (`model.txt` + `model.json`) |
| Config | `config/trading.toml` |
| Clips | Dota `base_size_usdc=50.0`, LoL `20.0` |
| Collector tag env | `POLYMARKET_TAG_ID` required, no default. Compose sets `"102366"` / `"65"`. |

Both trader services receive the **same** `DOTA_TRADING_MODE` / `LOL_TRADING_MODE`. `assigned_games(process_mode)` keeps only the matching token. Intended first paper deploy (document, do not run):

```
DOTA_TRADING_MODE=live
LOL_TRADING_MODE=paper
```

→ `trader-live` assigned `dota` only, `trader-paper` assigned `lol` only.

### 2.2 Names that are stale in today's docs (replace)

Do not leave these as operator instructions:

- Compose service `live-paper`
- Makefile `make live-paper`
- `LIVE_TRADING=1` as the live switch
- `ARCHIVE_ROOT=/archive` on the trader
- Collector services `archive` / `compact` (except historical "upgrade from old image" text in collector repo, which this story does not edit)
- `DOTA_TAG_ID`
- `config/dota-map.toml` as the live template path
- `load_current_model()` / live reading `data/new_model/current/`
- Feature row shape `(1, 11)` — code has 12 `FEATURE_COLUMNS`
- `match.json` v4 as the write schema — writes 5, accepts 3/4/5
- Restart table that only names `live-paper`
- Skill layout pointing at `data/live_paper` as the only tree **without** naming the two compose binds and the legacy tree

### 2.3 Names that stay (do not "modernize")

- Python package / module path `live_paper.*`
- CLI `prog="live-paper"`
- Log lines `live-paper assigned:`, `live-paper idle:`, `live-paper config loaded`, `live-paper session started:`, `live-paper session finished:`, `live-paper model startup blocked:`
- In-container `LIVE_PAPER_DIR = data/live_paper`
- Collector in-container `ARCHIVE_ROOT=/data` (that env is the collector's, not the trader's)
- `KALSHI_TRADING` tokens `off`/`observe`/`paper`/`live`. Independent of `--mode`. LoL-only process does not open Kalshi (US-007). Dota-live process still can.
- Steam feed facts, join-bid overlay, Kalshi overlay modules — keep unless a sentence names a stale file/service.

### 2.4 Running VPS vs compose file (must be explicit)

Until US-015, **both** are true. Docs that hide either will mislead the skill **today**.

1. **Compose file (code):** `trader-live` / `trader-paper`. This is what operators will run after rollout.
2. **Running container (now):** `dota_2_model-live-paper-1`, state `./data/live_paper`, Cmd without `--mode`. `live_paper_live` and `live_paper_paper` exist and are empty.

Every restart/deploy section starts with: do not recreate this checkout until the controlled US-015 rollout. A crash/`unless-stopped` reboot during a dirty tree is fail-closed, not a safe rollout — stop and report.

`summarize.py` must still find today's Dota matches in `data/live_paper` or the skill is broken the hour this merges.

### 2.5 Restart vs recreate (code, not the old env_file lore)

Compose dropped `env_file`. Environment is listed keys with `${…}` interpolation at **create** time.

| Change | Command | Why |
|---|---|---|
| Bind-mounted `src/`, `config/trading.toml`, `data/new_model`, `data/lol/models` | `docker compose restart trader-live` and/or `trader-paper` | Process re-imports. No rebuild. |
| `.env` `DOTA_TRADING_MODE` / `LOL_TRADING_MODE` / `KALSHI_*` / PK | `docker compose up -d --force-recreate trader-live trader-paper` | `restart` does not re-interpolate. |
| compose YAML command, mounts, service rename, first migration off `live-paper` | `up -d --force-recreate` those services; do **not** `restart` | YAML rename is not applied by restart of the old container. |
| Dockerfile, `pyproject.toml`, uv.lock, poly-maker copy | `docker compose build` then `up -d` | Image. There is no dota_2_model `.dockerignore`; a `--build` can bake host `.env` into `/app/.env`, and `env_value` would then feed `LIVE_TRADING`/`PK` to paper. Do not `--build` in this story. |
| Docs only | nothing on the VPS | |

Never `compose down`. Never restart without checking active matches (`summarize.py --live` plus logs). Restart detaches that market.

**Development rule (AGENTS.md, required):** this VPS checkout is bind-mounted into production. Between starting LoL work and the controlled US-015 recreate, do not `docker compose restart` / `up` / `--force-recreate` / `down` on `dota_2_model`. Bind-mounted files are visible on disk; WalletHost does not re-import running modules.

### 2.6 Exact commands the docs must contain

Document these. Do **not** run them in US-014.

**Initial LoL paper deploy** (after accepted production model is on disk — US-015 gate):

```bash
python3 /root/work/betting_workspace/.shared-skills/vps-live-paper/scripts/summarize.py --live
# If a Dota map is live, wait. Recreate detaches it.

# In /root/work/dota_2_model/.env (delete LIVE_TRADING entirely):
# DOTA_TRADING_MODE=live
# LOL_TRADING_MODE=paper
# Compose already sets DOTA_ARCHIVE_ROOT=/archive/dota and LOL_ARCHIVE_ROOT=/archive/lol.

cd /root/work/dota_2_model
docker compose up -d --force-recreate trader-live trader-paper
# Do not: docker compose restart
# The old live-paper container is an orphan after the YAML rename; US-015 removes it.
# Do not `up -d` without naming the two trader services while live-paper still runs.
```

**Verify assignment and no live LoL CLOB:**

```bash
docker compose -f /root/work/dota_2_model/compose.yaml ps
docker compose -f /root/work/dota_2_model/compose.yaml logs --since 5m trader-live
docker compose -f /root/work/dota_2_model/compose.yaml logs --since 5m trader-paper
# trader-live:  live-paper assigned: mode=live games=dota
# trader-paper: live-paper assigned: mode=paper games=lol
# neither: LIVE_TRADING is removed

docker compose -f /root/work/dota_2_model/compose.yaml exec trader-paper env | grep -E '^(PK|BROWSER_ADDRESS|LIVE_TRADING)=' || true
# empty: paper has no wallet secrets

# Paper session tape (host path after recreate):
# data/live_paper_paper/<match>/session.jsonl  session_start.execution_mode == paper
# PaperGateway fills are simulated; they are not CLOB. Absence of PK is the live-order gate.
```

**Rollback LoL** (collectors untouched):

```bash
# .env: LOL_TRADING_MODE=paper   # stay paper
#    or LOL_TRADING_MODE=off     # trader-paper idles (assigned=())
cd /root/work/dota_2_model
docker compose up -d --force-recreate trader-live trader-paper
```

`LOL_TRADING_MODE=live` is **not** this feature. Do not document it as a US-014/015 step. A short "later, not now" sentence is enough.

---

## 3. File-level changes

### 3.1 `/root/work/dota_2_model/AGENTS.md` — Live paper section (L159–188)

Rewrite that section. Leave Collect / Datasets / Train / Backtest / Agent rules unless a sentence is now false (the LoL Stage 05 paragraph at the top stays).

Must include:

- Two compose services, Makefile `trader`, required `--mode`.
- `DOTA_TRADING_MODE` / `LOL_TRADING_MODE` (`off`/`paper`/`live`; missing is an error).
- Archive mounts `/archive/dota` + `/archive/lol`; model mounts `data/new_model` + `data/lol/models`.
- Host state dirs `data/live_paper_live` vs `data/live_paper_paper`.
- Config `config/trading.toml`.
- Delete `LIVE_TRADING`. Keys alone never imply live; live process still needs `PK` + `BROWSER_ADDRESS`.
- Bind-mount rule: `src` / `config` / both model trees skip `--build`; process still loads template + models once at boot.
- **Do not restart/recreate this production-mounted checkout between development and the controlled rollout.** `restart: unless-stopped` after a crash is not that rollout.
- Pointer: deploy, verify-no-live-LoL, rollback, and the restart table live in `docs/live-paper.md`.
- Replace `docker compose up -d` / `logs -f` / `make live-paper` / `ARCHIVE_ROOT` in this section.

Keep the docs index table. `docs/live-paper.md` blurb can mention two games / GRID LoL.

Do not paste the full Kalshi overlay. Do not duplicate the 1k-line live-paper historical notes.

### 3.2 `/root/work/dota_2_model/docs/live-paper.md`

This file is long. Do **not** rewrite Steam-feed measurements or the US-011 historical helper notes. Patch operator-wrong sentences and add a short two-game operations section.

**Must update (search these; they are currently wrong):**

- Discovery still talking about unsuffixed `ARCHIVE_ROOT` as the trader env. Per-game `load_archive_root(profile)`. Sidecar path is still `<archive>/metadata/markets/*.json`.
- `match.json` write schema 5 + `game`. Accept 3/4/5. Missing `game` on read → `dota`. Boot scan already accepts 5.
- `load_production_model()` no-arg / `load_current_model()` / live `current/` → `load_production_model(profile)` from `profile.production_model_dir`. WalletHost map `game → model`.
- Config path `dota-map.toml` → `trading.toml`. `read_template(games)`. Wallet materialize picks largest clip (`dota-map` when both). Match attach uses that game's `profile_name`.
- `execution_mode()` reads `LIVE_TRADING` → CLI `--mode`. First-live paragraph that says set `LIVE_TRADING=0` then `1`.
- Model registry "Live loads `data/new_model/current/`" (the operations block around L564). Dota production is `data/new_model/production/`. LoL is `data/lol/models/production/`. Missing pair blocks startup (`live-paper model startup blocked:`). **Do not recreate traders until the LoL production pair exists and was accepted** (US-015). This story only documents the gate.
- Feature row `(1, 11)` → `(1, 12)` (`FEATURE_COLUMNS` in `shared.utils.gbm`).
- GRID picker: LoL is GRID-only (`uses_steam=False`); Dota still delay-compares. `GridFrameReducer` / `GridLiveFeed` take `GameProfile`. Sides from `side_0_text`/`side_1_text` (BLUE/RED vs RADIANT/DIRE). XP from `profile.level_xp`. LoL top1 is `/ team total`.
- Gold-velocity: Dota 350 / 30s; LoL profile cap `None` skips `MISSING_NW` and `NW_VELOCITY`.
- "Where it runs" / "What changed — what to restart": two services, two host state dirs, recreate vs restart table from §2.5, production-mounted-checkout rule, exact deploy / verify / rollback from §2.6.
- Size paragraph: two clips; risk dollars from the **sum** of loaded games; per-profile `q_max_usdc` from that profile's clip. Do not claim a single `base_size_usdc` is the only money number.
- `KALSHI_TRADING` "Independent of `LIVE_TRADING`" → independent of `--mode`. LoL-only process does not load Kalshi. `[kalshi]` still in `trading.toml`.
- Archive layout: show host `data/live_paper_live` and `data/live_paper_paper`, and that the process still writes `data/live_paper/<match_id>/` **inside** the container. Mention leftover host `data/live_paper` is the pre-rollout tree; do not copy it in this story.
- `GameProfile` pointer: frozen dataclass, `GAME_PROFILES`, no registry class.

**Add** a compact `## Two games (operator)` (or fold into `## Live paper operations`) covering: GameProfile, two processes, GRID-only LoL, explicit off/paper/live, production model gate, commands from §2.6.

**Do not** retell every US-011 review round. Do not invent Kalshi LoL. Do not rename the Python package in prose as if it happened.

### 3.3 `/root/work/betting_workspace/.learnings/dota_2_model.md`

Replace the live-daemon bullets and the **What to restart** table.

Table columns: what changed → which service(s) → restart vs recreate → running matches die.

Rows at least:

- `src/` Python, `config/trading.toml`, Dota `data/new_model/production`, LoL `data/lol/models/production` → `restart` the process that loaded it (`trader-live` and/or `trader-paper`) after `--live` is empty
- `.env` modes / Kalshi / PK → `up -d --force-recreate trader-live trader-paper`
- compose command/mounts/service split / first leave of `live-paper` → recreate, not restart
- deps / Dockerfile / poly-maker copy → build then up
- docs only → nothing

Also: container names after rollout `dota_2_model-trader-live-1` / `dota_2_model-trader-paper-1`; until US-015 the running name is still `dota_2_model-live-paper-1`. Bind mounts. `trading.toml`. Kalshi still Dota-only. Boot line still `live-paper config loaded …`. `KALSHI_TRADING` independent of `--mode`. Production-mounted checkout: no restart until controlled rollout.

Keep the Kalshi module map; retarget TOML path `config/trading.toml`. Sqlite path: in-container `data/live_paper/wallet/…`; host live vs paper binds.

### 3.4 `/root/work/betting_workspace/.learnings/polymarket-collector.md`

Replace "Dota 2 markets" / `tag_id=102366` as the only discovery story.

Must state:

- One binary, four compose services: `archive-dota`, `compact-dota`, `archive-lol`, `compact-lol`.
- `POLYMARKET_TAG_ID` is required (no default). Empty/whitespace rejected. Compose sets `"102366"` (dota) and `"65"` (lol).
- Two host roots: `/var/lib/polymarket-dota-archive`, `/var/lib/polymarket-lol-archive`. In-container both still `ARCHIVE_ROOT=/data`.
- Only `archive-dota` has `build: .`; all four share `image: polymarket-collector:latest`.
- Traders mount those roots read-only at `/archive/dota` and `/archive/lol`.
- Compact is offline parquet; it does not affect quoting. Fresh sidecars (mtime last 2h) live under `metadata/markets/*.json`.

Do not claim v1 default tag 102366. Do not tell operators to set the tag only in `.env` when compose already pins it per service.

### 3.5 `.shared-skills/vps-live-paper` (hardlinked)

**SKILL.md**

- Frontmatter `description`: two games, `trader-live`/`trader-paper`, four collectors. Keep "do not restart unless asked".
- Layout table: two traders, four collectors, two host state dirs, legacy `data/live_paper` until US-015, two model dirs, `trading.toml`, two archive roots at `/archive/dota` and `/archive/lol`.
- First commands: `ps` both composes; logs `trader-live` and `trader-paper`; **until rollout also** `logs --since 30m live-paper` because that is what is running.
- Restart section: check `--live` (and logs) first; say the match is live before restarting; `restart` only bind-mount code/model; env/service migration is `up -d --force-recreate`; never during the development window before US-015; never `down`.
- Size change: `config/trading.toml` `[profiles.dota-map]` / `[profiles.lol-map]`. Restart the process that loaded that profile.
- Collector: four service names; check the archive for **that game**.
- Game-specific: answer Dota vs LoL separately (process, state dir, model, gold-velocity, GRID-only LoL, Steam Dota).
- Include §2.6 verify / rollback as copy-paste (skill is what agents run on this VPS).

Keep PnL rules. They did not change.

**log-map.md**

- Paths: host live/paper/legacy trees; in-container still `data/live_paper/<id>/`.
- `match.json` schema 5 + `game`.
- `session.jsonl` schema 6 (already on tape; the map still says schema 5).
- LoL archives `grid_state.jsonl`; Steam still `state.jsonl`.
- `nw_velocity` is Dota-only at cap 350; LoL skips that gate.
- Telegram prefix remains `live-paper session started:` (includes `mode live|paper`).

**scripts/summarize.py**

This is the "game-specific summaries" bullet. Smallest change:

- Roots, in order, skip missing/empty: `data/live_paper_live`, `data/live_paper_paper`, `data/live_paper` (legacy, required until US-015).
- `--game dota|lol` filters `match.json` `game` (missing → `dota`).
- Print `game` and which host tree on each row.
- `--wallet` / `polymarket_today`: first existing `wallet/live.db` among live then legacy (paper.db is not the Polymarket live funder).
- `--match` searches all roots.
- Keep `--self-check`. Run it.

Do not add argparse frameworks or extra files. Do not hit the network in `--self-check`.

### 3.6 Do not touch

- `compose.yaml`, Makefile, Dockerfile, `.env`, Python under `src/`
- `poly-maker`
- `../polymarket-collector/**` (including `docs/docker-compose.md` — see surprises)
- `scripts/sync_live_paper.py` (still rsyncs `data/live_paper`; leftover, not this story)
- `docs/next_steps.md`, `old_docs/`
- US-015: no recreate, no model-contract gate execution, no `.env` write

---

## 4. Tests / checks

Docs-only plus `summarize.py`. Do not "fix" compose tests.

From `/root/work/dota_2_model`:

```bash
PYTHONPATH=src:scripts:../prediction-market-backtesting uv run --group backtest python -m pytest \
  tests/test_trader_compose.py -q
make lint-all
uv run python -m basedpyright
```

From `/root/work/polymarket-collector` (no file changes; prove still green):

```bash
yarn typecheck
yarn check
```

From betting_workspace:

```bash
python3 .shared-skills/vps-live-paper/scripts/summarize.py --self-check
ls -i .shared-skills/vps-live-paper/SKILL.md .cursor/skills/vps-live-paper/SKILL.md
```

Do **not** run `docker compose up|down|restart`. If you glance at the running trader it must still be Id `a188e0d8ebb5…` / StartedAt `2026-08-30T07:23:39Z` / name `dota_2_model-live-paper-1`. Collectors stay the four `*-dota`/`*-lol` services.

Grep gates before commit (from each repo as needed):

```bash
# dota_2_model docs must not instruct these as current operations:
rg -n 'make live-paper|restart live-paper|service `live-paper`|LIVE_TRADING=1|ARCHIVE_ROOT=/archive[^-]|config/dota-map.toml|load_current_model|data/new_model/current/' \
  AGENTS.md docs/live-paper.md

# must contain:
rg -n 'trader-live|trader-paper|DOTA_TRADING_MODE|LOL_TRADING_MODE|trading.toml|live_paper_live|live_paper_paper|/archive/dota|/archive/lol' \
  AGENTS.md docs/live-paper.md

# betting_workspace
rg -n 'trader-live|archive-dota|POLYMARKET_TAG_ID|live_paper_live' \
  .learnings/dota_2_model.md .learnings/polymarket-collector.md \
  .shared-skills/vps-live-paper/SKILL.md
rg -n 'services `archive`, `compact`|service `live-paper`|dota-map.toml' \
  .learnings/dota_2_model.md .learnings/polymarket-collector.md \
  .shared-skills/vps-live-paper/SKILL.md
```

Historical mentions of `live-paper` **log prefix** / Python module are allowed. Operator commands that restart `live-paper` as if it were the compose service are not, except one explicit "until US-015 the running container is still `live-paper`" sentence.

---

## 5. Commit strategy

Work on `main`. Do not commit broken docs. Do not `--amend` unless the implement-step hook rules apply. Do not push unless the user asks. Do not `--no-verify`.

### 5.1 `dota_2_model` — one commit

Stage only:

- `AGENTS.md`
- `docs/live-paper.md`

```
feat: US-014 - Обновить operator docs и VPS diagnostics под две игры

Operator runbook now matches trader-live/trader-paper, per-game modes,
and the two archive/model mounts; do not recreate until the controlled rollout.
```

### 5.2 `betting_workspace` — after dota_2_model is green

Per `feature-json-implement-step`:

1. Set `userStories[id=US-014].passes` to `true`. Do not flip US-015. Do not revert US-001..US-013.
2. **Append** (never replace) `current-task/progress.txt` with `## [Date/Time] - US-014`. Useful learnings: compose names vs still-running `live-paper`; CLI/log prefix still `live-paper`; host binds vs in-container `data/live_paper`; `restart` vs `--force-recreate`; skill files are hardlinks; `summarize.py` must keep the legacy tree until US-015; collector `docs/docker-compose.md` still stale (yarn check pins it); do not bake `.env` with `--build`.
3. Commit learnings, the skill tree (one inode), `current-task/feature.json`, `progress.txt`, `current-task/plans/US-014.md`. Same message as §5.1.

---

## 6. Risks

| Risk | What to do |
|---|---|
| Docs only name `trader-*`, skill stops finding today's Dota tape | Keep legacy `data/live_paper` in summarize + one until-rollout sentence. |
| Docs only name `live-paper`, post-US-015 operators restart the wrong service | Compose file is the target; running name is a footnote. |
| `up -d` without service names while `live-paper` still runs | Document the fork: two wallets. Named `trader-live trader-paper` plus US-015 orphan removal. |
| Telling people `docker compose restart` for `.env` modes | Recreate. Restart does not re-interpolate. |
| Rewriting all of live-paper.md | Patch stale operator facts + one two-game section. |
| "Fixing" CLI prog / log prefix / package name | Out of scope. Those strings are still code. |
| Editing `.env` or applying compose "to match the new docs" | US-015. File-only. |
| Four skill copies edited as four files and inodes split | Write the `.shared-skills` path; confirm `ls -i`. |
| Updating collector `docs/docker-compose.md` | `docker.test.ts` still asserts `logs -f archive compact`. Leave collector repo alone. |
| Claiming LoL paper fills are bugs | PaperGateway writes `fill` rows. No PK is the CLOB gate. |
| `(1, 11)` left in place | Shared features are 12. |
| `make lint-all` on unstaged md | `git add` first. |

---

## 7. Acceptance checklist (from `feature.json` US-014 `changes[]`)

Copy. Implementer ticks against the diff, not memory.

- [ ] `../dota_2_model/AGENTS.md`: new trader commands, mode variables, archive/model mounts, rule not to restart the production-mounted checkout between development start and controlled rollout
- [ ] `../dota_2_model/docs/live-paper.md`: GRID-only LoL path, GameProfile, two processes, separate state directories, explicit off/paper/live, production model gate
- [ ] `.learnings/dota_2_model.md`: table of what to restart for `trader-live`/`trader-paper` and model/config/code rollout rules
- [ ] `.learnings/polymarket-collector.md`: four service names, required `POLYMARKET_TAG_ID`, two archive roots
- [ ] `.shared-skills/vps-live-paper`: new service names, data roots, game-specific summaries, safe restart/recreate only after checking active matches
- [ ] Docs contain exact commands for initial paper deploy, verifying no live LoL orders, and rollback to `LOL_TRADING_MODE=paper` or `off`
- [ ] Every mentioned compose service name, path, and CLI command checked against the final code (`compose.yaml`, Makefile, orchestrator, trading_mode, game_profile, collector compose)
- [ ] Tests pass (`test_trader_compose.py`, summarize `--self-check`)
- [ ] `make lint-all` passes
- [ ] `yarn check` passes
- [ ] Typecheck passes (`basedpyright`, `yarn typecheck`)

Plus implementer-owned (skill / this plan, not in `changes[]`):

- [ ] No compose up/down/restart/recreate; live-paper Id/StartedAt/name unchanged
- [ ] `.env` / compose / Makefile / Dockerfile / Python / `poly-maker` / polymarket-collector sources untouched
- [ ] Skill hardlinks intact
- [ ] `US-014.passes: true`; progress appended; one dota_2_model commit + one workspace commit
- [ ] US-015 not started
