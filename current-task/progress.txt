## 2026-09-03 19:32 CEST - orchestrate start

- Archived completed live-lol feature to `docs/completed-tasks/live-lol/`.
- New feature: canonical pipeline cleanup in `../dota_2_model`.
- Order: US-001 four-shard launcher, US-002 LoL forced-hold, US-003..US-009 collapse Tasks 1–7.
- Product git lives in `../dota_2_model` on `main`. Preserve `.streamlit/config.toml` deletion and `docs/next_steps.md`.
- poly-maker frozen. No full prepare/train/backtest jobs.
- Pending: US-001.

## 2026-09-03 19:52 CEST - US-001

- Fixed `scripts/run_seeds.sh` at four shards: `SHARDS=4` script-scope, `shift 2`, usage `<dota|lol> <name> [extra flags]`. Integer shard validation deleted. AGENTS.md + domain.md say fixed at 4; Dota/LoL examples dropped trailing `4`. Cold-cache 3-shard example left. Plan file staged. Product commit `e6aae12` in dota_2_model. `passes` still false.
- **Learnings for future iterations:**
  - `local SHARDS=4` is illegal at bash script top-level; keep it script-scoped.
  - Old trailing `4` is now an extra flag forwarded to argparse and fails on purpose. Do not smoke-test with a real `<name>` (starts 12 backtests).
  - Do not stage sibling 2026-09-03 plans, `.streamlit/config.toml`, or `docs/next_steps.md`.

## 2026-09-03 19:55 CEST - US-001 review

- Review: no comments. US-001 passes true. Product commit `e6aae12`. Pending: US-002.

## 2026-09-03 20:21 CEST - US-002

- Implemented all three source-plan Tasks. LoL replay always ends at the last livestats frame; `still_going` / `cutoff_ts` / `LOL_MARKET_END_SECOND` gone. Forced-hold fields deleted from `MarketContext`, `MakerMatchResult`, wallet, summaries, reports. Checkpoint resume selects current dataclass fields and ignores extra legacy columns. `LOL_PREPARE_END_SECOND = LOL_FETCH_END_SECOND`. `buy_cutoff_ns` / `game_end_ns` / `_on_game_end` kept. Product commits in dota_2_model: `9b897c1`, `dc1461c`, `b275ef5`. Forced-hold plan staged with Task 3. `passes` still false.
- **Learnings for future iterations:**
  - `read_results_checkpoint` is column selection, not fill-defaults. Invert of the old missing-column test: write extras, assert equality with the current dataclass.
  - Task 3 `rg` still hits tests on purpose: negative schema asserts, `removed.isdisjoint`, and the legacy-column checkpoint fixture. Production `src` / `scripts` / `AGENTS.md` / `docs/domain.md` are clean.
  - Collapse plan, `.streamlit/config.toml` deletion, and `docs/next_steps.md` left unstaged. `uv run pytest` needs `PYTHONPATH=src` (Makefile export).

## 2026-09-03 20:23 CEST - US-002 review

- Review: no comments. US-002 passes true. Product commits `9b897c1`, `dc1461c`, `b275ef5`. Remaining steps skip planning: implement then review. Pending: US-003.

## 2026-09-03 20:35 CEST - US-003

- Removed model-bank routing. Canonical predict loads one `model.txt`; sibling `bank.json` cannot redirect. Deleted `BANK_FILENAME`, `BankRoute`, `load_bank_routes`, `assign_bank_routes`, `predict_bank_deltas`, `ModelBank`, `bank_fingerprint`, `tests/test_backtest_bank.py`. LoL manifest fingerprints `model_sha256` only. Product commit `c9d91d9` in dota_2_model. `passes` still false.
- **Learnings for future iterations:**
  - TDD: `test_predict_model_deltas_always_loads_model_txt` with a fake bank pointing at `late.txt` fails first because current code loads `late.txt`.
  - `test_lol_run_manifest_fingerprints_inputs` lives in `tests/test_backtest.py`; LoL `build_run_manifest` hashes split/signals/market/audit paths, so stub those constants.
  - Step 5 `rg` still hits the negative `"bank_sha256" not in manifest` assert. Production `src` is clean.
  - `uv run pytest` needs `PYTHONPATH=src:scripts:../prediction-market-backtesting uv run --group backtest`. Collapse plan, `.streamlit/config.toml`, `docs/next_steps.md` left unstaged.

## 2026-09-03 20:38 CEST - US-003 review

- Review: no comments. US-003 passes true. Product commit `c9d91d9`. Pending: US-004.

## 2026-09-03 20:55 CEST - US-004

- Fixed Dota at 300s, minute rows `-60..540`. Deleted horizon/window tooling (`horizon_window_dataset`, `horizon_label_grid`, `HorizonWindowTrainRow`, `build_horizon_window_train_rows`, `--target-horizon-seconds`, `--train-second-max`, `require_live_research_defaults`). Dota labels via `build_price_delta_labels`. Backtest inference uses `select_usable_validation_rows` (full map). Product commit `9d71f6d` in dota_2_model. `passes` still false.
- **Learnings for future iterations:**
  - Path/`--production` stay until US-005. `build_horizon_price_delta_labels` stays until US-006 (LoL).
  - `docs/experiments/dota-minute-specialists/{train,sweep,check_m420}.py` call the Dota helpers; basedpyright typechecks them, so extra horizon/window args had to drop in this commit.
  - Collapse plan, `.streamlit/config.toml`, `docs/next_steps.md` left unstaged.

## 2026-09-03 21:00 CEST - US-004 review

- Applied blocking: `load_training_dataset` now returns `frame[TRAINING_COLUMNS]` only. Deleted `test_load_training_dataset_keeps_extra_horizon_labels`. Product commit `a29c85b`. `passes` still false.
- Skipped nits: minute-specialist signature drops stay; reviewer said they are required for lint, not extra scope.
- **Learnings for future iterations:**
  - After deleting a parquet writer, grep the loader that kept its extra columns. Task 7 `rg` for names will miss a generic `signal_market_p_radiant_` extras branch.

## 2026-09-03 21:02 CEST - US-004 pass

- Review findings applied once (`a29c85b`). US-004 passes true. Pending: US-005.

## 2026-09-03 21:07 CEST - US-005

- One Dota prepare writes research training/validation/split plus production training/split. One train publishes research then production with the research tree count. Deleted `--production`, `DatasetOutputs`, `prepare_output_paths`, `prepare-production`, `train-production`, `RESEARCH_MODEL_META_PATH`. Kept `research_boost_rounds` for LoL until US-006. Product commit `6fa6ca8` in dota_2_model. `passes` still false.
- **Learnings for future iterations:**
  - `train_production(..., *, boost_rounds=)` is keyword-only; the order test stubs it that way. `main(["--production"])` must raise before LightGBM, so parse_args runs first.
  - Existing research orchestration test calls `train_research` with explicit paths, not `main()`, because `main()` now also trains production.
  - Step 8 `rg` still hits the negative `--production` CLI asserts. Production `src` / Makefile are clean.
  - Collapse plan, `.streamlit/config.toml` deletion, and `docs/next_steps.md` left unstaged.

## 2026-09-03 21:10 CEST - US-005 review

- Review: no comments. US-005 passes true. Product commit `6fa6ca8`. Pending: US-006.

