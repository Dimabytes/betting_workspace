## 2026-08-24 22:38 CEST - US-001
- Split SOURCE_LAG_SECONDS=10 from Steam/backtest 1s polls; dropped poll_interval_seconds from ModelMeta and the live contract; recorded lag and backtest poll on the run manifest.
- Rebuilt datasets, trained research `20260824T200814Z`, five-shard s2-join validation backtest, compared vs lag-2 research `20260824T143419Z` (production `20260824T152512Z` has no validation backtest).
- User dropped the b0 arm; compare used `--arm s2` only.
- **Learnings for future iterations:**
    - Patterns: `make backtest ARGS="--validation --profile s2-join --shard i/5 --name …"` then `--merge-shards 5`; `compare_backtests.py --arm s2` filters two-arm baselines.
    - Gotchas: `assert_manifest_matches` is exact-key; old checkpoints cannot `--resume` after lag/poll keys land. Production lag-2 now fail-closes on `source_lag_seconds`. `window_seconds` moves 8s/match with PREHORN_LEAD 58→50.
    - Context: candidate s2 PnL +$592 vs baseline +$1179 on 432 shared matches; not a quality gate.
---

## 2026-08-24 23:24 CEST - US-002
- Threaded nullable `steam_delay_s` through discovery. Added `LiveFeed`/`FeedEvent`/`MatchPhase`. `SteamLiveFeed` owns `state.jsonl`. MatchWorker iterates the feed. Watchdog timeout is a constructor arg (Steam 3s).
- **Learnings for future iterations:**
    - Patterns: feed adapter writes its own archive then yields; `WalletHost._launch` is the picker seam.
    - Gotchas: `steam_delay_s` must be `compare=False` or match.json v1 resume refuses to rebind. Do not parse delay with `require_nullable_int` (raises drop the GRID row). `steam_feed` cannot import `state_writer`.
    - Context: GRID feed and match.json v2 are US-003/US-004. No daemon run; production is still lag-2 fail-closed.
---

## 2026-08-24 23:58 CEST - US-003
- Moved GRID widget parse into `shared.utils.grid_widgets`. Watcher imports it. Added `GridLiveFeed.read_frame`/`ticks`. Tests are the only caller; `wallet_host.py` untouched.
- **Learnings for future iterations:**
    - Patterns: `score_team_name` observed names are already `normalize_team_name`'d. `read_frame(raw, now)` is the test seam. Fixtures in `tests/grid_widget_fixtures.py`.
    - Gotchas: `same_table_content` must ignore `feed_delay`. Terminal is map `status == finished`, not series. Horn uses raw `currentSeconds`, not the extrapolated clock.
    - Context: no live Dota 2 series during verify; Gamma list path works (0 events). US-004 archives inside the feed. US-005 wires it in `_launch`.
---

## 2026-08-25 00:30 CEST - US-004
- GRID `ticks()` writes every raw socket frame to `grid_state.jsonl` before parse. match.json is schema 2: required `feed_source`, nullable `server_steam_id`, delay keys always present. Steam observed-lag median is state 5 only. GRID finalize replays `read_frame`; pause/missing are JSON null. Boot scan ignores schema != 2. `_launch` skips a null server id (GRID-only attach is US-005).
- **Learnings for future iterations:**
    - Patterns: feed owns its jsonl; finalize branches on `feed_source`. `GridLiveFeed(..., match_id)` is required. `TeamSide.won` is the GRID winner field, only when map status is finished.
    - Gotchas: v1 leftover with `final` set is not a boot target. `steam_delay_s` stays `compare=False` so a later discovery delay cannot rebind. First v2 rollout moves old match dirs out of `data/live_paper` before restart.
    - Context: `_launch` is still Steam-only. US-005 picks the feed and pins `feed_source` on the first event.
---

## 2026-08-25 00:56 CEST - US-005
- Added `MAX_FEED_DELAY_SECONDS`, `source_picker.pick_source` / `probe_grid_delay`, and `pinned_feed_source`. Discovery emits GRID-only when `grid_series_id` is set. WalletHost picks once, pins the source, and does not restart a GRID worker on a new Steam server id. A picker skip does not consume a crash restart.
- **Learnings for future iterations:**
    - Patterns: picker lives in `source_picker.py`; `_launch` only mkdirs and spawns `_pick_and_run`. Probe is inside the match task so discovery is not stalled.
    - Gotchas: conflicting GetTopLiveGame ids now drop Steam only — a sidecar with `gridSeriesId` still emits. `no_source` must clear in `_reap` before the crash-restart path. Pin reads `match_meta.LIVE_PAPER_DIR`, not the wallet_host copy.
    - Context: no daemon run; production stays lag-2 fail-closed. Probe opens a second short-lived socket; `GridLiveFeed.ticks()` opens its own.
---

