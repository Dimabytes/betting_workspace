## 2026-08-30 11:48 CEST - US-001

- Added compact CBLOL (`lol-fxw7-los-2026-08-29`) and LEC (`lol-navi-gx-2026-08-29`) scoreboard + series_table fixtures in `dota_2_model/tests/lol_grid_widget_fixtures.py`.
- Tests run them through existing `parse_frame`, `read_map_scoreboard`, `read_net_worth`. Production parser unchanged.
- Checks: BLUE/RED sides, GAME_STATE_GROUP / PLAYER_ENTITY_GROUP, entity.teamColor `teams.<id>`, NetWorth/Kills/Deaths/KillAssistsGiven, increaseLevel absent before first level-up and present after, two sides / ten players / recorded NetWorth and Deaths sums.
- **Learnings for future iterations:**
  - GRID LoL portraits are character UUIDs; `hero_from_icon` returns "" and the parser still yields gold.
  - GRID omits the whole `increaseLevel` column until the first level-up (not null cells). After it appears, some cells can still be null.
  - LEC map 2 keeps `activeGameIndex=1`; NAVI was RED and GIANTX BLUE. Side is not team-order.
  - Compact fixtures drop extra LoL columns (`ExperiencePoints`, `Money`, `LoadoutValue`, objectives). Parser only reads Game/Player typed fields.
  - Production checkout had no leftover uncommitted Kalshi files; Kalshi work is already on main (`fix kalshi`).
  - `make lint-all` skips untracked files: git add new tests before lint.

## 2026-08-30 11:57 CEST - US-002

- Collector tag is `Settings.polymarketTagId` / required `POLYMARKET_TAG_ID` (no default). Empty/whitespace fail `validateSettings`; missing/empty env fail at Config load.
- `compose.yaml` has `archive-dota`/`compact-dota` (tag 102366, dota archive) and `archive-lol`/`compact-lol` (tag 65, lol archive). One `build: .` on `archive-dota`. Not applied.
- Checks: targeted config/docker/app tests, `yarn typecheck`, `yarn check` (434 tests), `docker compose config --services` shows four services. Running stack still `archive`/`compact` Up ~8 days.
- **Learnings for future iterations:**
  - Effect empty env ≡ missing, so empty `POLYMARKET_TAG_ID` fails at Config load; whitespace is what hits `validateSettings` trim.
  - `app.test.ts` `withArchiveRoot` must supply `POLYMARKET_TAG_ID` or almost all daemon tests fail missing-key.
  - Production still runs `archive`/`compact` until US-003. Editing compose on disk does not recreate them.
  - `docker.test.ts` runbook half still documents old `archive`/`compact` names; do not update runbook until US-014.
  - `JSON.stringify` in config error text fails Effect `preferSchemaOverJson`; quote the raw value instead.
- Review: no comments.

## 2026-08-30 12:06 CEST - US-003

- Created `/var/lib/polymarket-lol-archive` as 0755 uid/gid 10001 (same as Dota). Applied collector compose with `--build --remove-orphans`. Did not touch dota_2_model or live-paper.
- Compose services now: `archive-dota`, `compact-dota`, `archive-lol`, `compact-lol` (all Up). Old `archive`/`compact` containers removed. No leftover `polymarket-collector-archive-1` / `compact-1`.
- LoL archive: 133 event sidecars all `tagId=65`, 324 market sidecars (`lol-*` slugs), book journal `/var/lib/polymarket-lol-archive/events/2026-08-30/10.jsonl.partial` growing (mtime 12:06:31 CEST, size ~88MB and still appending).
- Dota archive kept writing after recreate: sidecars mtime 12:04:45 CEST (was 12:02:09 pre-deploy); journal `10.jsonl.partial` grew ~33MB → ~92MB by 12:06:31. Events stay `tagId=102366`.
- live-paper before: Id=`a188e0d8ebb54486ad67e1722e5e207d283d5f442a258aabc1291046c44ef6bc` StartedAt=`2026-08-30T07:23:39.75028444Z` running. After: same Id and same StartedAt, still running.
- Host `yarn typecheck` exit 0; `yarn check` 434 passed (0 errors). Image build also ran `yarn check` 434 passed.
- **Learnings for future iterations:**
  - Compose service rename leaves old `archive`/`compact` as orphans; `--remove-orphans` is required so `ps` has no old names.
  - Event sidecars store `tagId` at the wrapper root and Gamma `event.tags[].id` (65 = league-of-legends). Market sidecars do not repeat the numeric tag.
  - First LoL poll can warn on one decode issue (`clobTokenIds` not string) and still subscribe hundreds of markets; one issue is not an empty archive.
  - Collector recreate does not restart live-paper if you only run compose in `/root/work/polymarket-collector`.

## 2026-08-30 12:09 CEST - audit US-001–US-003

- Independent read-only audit: AUDIT CLEAN. US-001/002/003 remain passes true. live-paper Id/StartedAt unchanged. Four collector services running; both archives fresh.

## 2026-08-30 12:17 CEST - US-004

- Added `src/live_paper/game_profile.py`: frozen `GameProfile` + static `GAME_PROFILES` for `dota` and `lol`. No factory, no callers.
- Tests cover exact key set, field identity vs imported constants, FrozenInstanceError, and no shared mutable collections.
- Checks: 5 pytest passed; `make lint-all` passed; `basedpyright` 0 errors. live-paper Id=`a188e0d8ebb5…` StartedAt=`2026-08-30T07:23:39Z` unchanged.
- **Learnings for future iterations:**
  - Import `LEVEL_XP` / `LOL_LEVEL_XP` / `PRODUCTION_MODEL_DIR` / `LOL_PRODUCTION_MODEL_DIR` / `MAX_ABS_NW_DELTA_30`; do not copy tables or reconstruct Paths.
  - `GAME_PROFILES` is unused until US-005+; bind-mounted `src/` is visible in the container but live-paper does not need a restart.
  - `make lint-all` skips untracked files: `git add` the new module before lint.
  - `object.__setattr__` bypasses frozen dataclasses (no slots); call `profile.__setattr__` to assert `FrozenInstanceError` without a direct assignment basedpyright rejects.
  - Dota sides are locked to `RADIANT_SIDE` / `DIRE_SIDE` in tests; LoL `"BLUE"` / `"RED"` stay profile field literals (do not add them to `grid_widgets.py` until US-011).
- Review: no comments.

## 2026-08-30 12:36 CEST - US-005

- Renamed `config/dota-map.toml` → `config/trading.toml`. Added `[profiles.lol-map]` with `base_size_usdc=20.0` and the same key set; Dota clip stays 50.
- `read_template(games)` pins exactly those `profiles.*` tables. Risk dollars from the sum of loaded clips; each profile's `q_max_usdc`/`merge_min_size` from its own clip. Wallet materialize picks the largest clip (`dota-map` when both).
- Production callers pass both GameProfiles. `match_worker` still hardcodes `"dota-map"`. No compose restart. live-paper Id=`a188e0d8ebb5…` StartedAt=`2026-08-30T07:23:39Z` unchanged.
- Checks: 98 pytest passed; `make lint-all` passed; `basedpyright` 0 errors.
- **Learnings for future iterations:**
  - compose already mounts `./config` as a directory, so the rename needs no YAML change.
  - Running live-paper still has the old `dota-map.toml` in memory; a rename on disk is not a rollout.
  - Production must pass both profiles against the committed file or boot crashes on extra `lol-map`.
  - `DiscoveredMatch` has no `game` field yet; `materialize_config_dir` takes `game: str`.
  - `make lint-all` skips untracked/renames: `git add` the `git mv` before lint.
- Review: no comments.

## 2026-08-30 12:53 CEST - US-006

- CLI `--mode live|paper` is required. `execution_mode` no longer reads `LIVE_TRADING`. `assigned_games` validates both `DOTA_TRADING_MODE` / `LOL_TRADING_MODE` (missing/empty/unknown fail; explicit `off` unassigns). `reject_legacy_env` fails if `LIVE_TRADING` is set at all.
- `run_wallet_daemon` validates env then still opens Steam/Kalshi/wallet for both games. Compose `live-paper` gained `command: … --mode live` (file only). Makefile `live-paper` appends `--mode paper`. Dockerfile and `.env` untouched.
- Checks: 93 pytest passed; `make lint-all` passed; `basedpyright` 0 errors. live-paper Id=`a188e0d8ebb5…` StartedAt=`2026-08-30T07:23:39Z` Cmd still without `--mode`.
- **Learnings for future iterations:**
  - Running container Cmd has no `--mode`. `restart: unless-stopped` reuses that stored Cmd; compose `command:` is not applied until recreate (US-015). Do not `docker compose up`.
  - Host `.env` still has `LIVE_TRADING=1` and neither per-game var. A new process fail-closes even with `--mode`. Do not edit `.env` until US-015.
  - Tests must patch `live_paper.trading_mode.env_value`; `env_value` reads repo `.env`.
  - Kalshi unset/blank=`off` must not be copied; missing per-game mode is a startup error.
  - `assigned_games` result is discarded until US-007; materialize still gets both GameProfiles.
  - `conftest` now `delenv LIVE_TRADING`; any assigned value (including `"0"`) trips `reject_legacy_env`.
  - `make lint-all` skips untracked files: `git add` first.

## 2026-08-30 13:00 CEST - US-006 review

- Renamed autouse fixture `force_paper_trading_flag` → `clear_legacy_live_trading`. Commented `assigned_games(mode)` as validate-only until US-007 (did not bind unused `games`; ruff F841).
- Skipped wallet_host 1k-line split: this story is 995 lines; moving boot policy is US-007 work.
- Checks: 93 pytest passed; `make lint-all` passed; `basedpyright` 0 errors. US-006 stays passes true.
- **Learnings for future iterations:**
  - US-007 should consume `assigned_games` in `trading_mode` / a small helper, not grow `wallet_host.py` past 1k.

## 2026-08-30 13:19 CEST - US-007

- Extracted `open_wallet_host` + `run_wallet_daemon` into `host_resources.py`. `wallet_host.py` is 882 lines. `WalletHost.steam_client` may be None; LoL-paper `run()` waits instead of constructing `MarketDiscovery`.
- `steam_required` follows `uses_steam`; `kalshi_required` follows assigned Dota. Dota-live opens CLOB wallet + Steam + Kalshi load. LoL-paper skips those three and still flocks the paper wallet with LoL clips. Empty assignment logs `mode=` + `assigned=()`, stays alive, opens nothing (no archive root either).
- `read_template` extra profile tables are ignored; missing still errors. Committed `trading.toml` can serve Dota-only or LoL-only.
- Checks: 123 pytest passed; `make lint-all` passed; `basedpyright` 0 errors. live-paper Id=`a188e0d8ebb5…` StartedAt=`2026-08-30T07:23:39Z` unchanged. Commit `fc3e657`.
- **Learnings for future iterations:**
  - Extract `open_wallet_host` into `host_resources.py` because `wallet_host.py` was 995 lines; `host_resources` imports `WalletHost`, never the reverse.
  - `read_template` must allow extra profile tables so one `trading.toml` serves both processes.
  - LoL-only must not call `load_kalshi_settings` (shared env has Kalshi keys).
  - Empty assignment must skip `load_archive_root`.
  - `MarketDiscovery` still requires Steam — LoL-paper `run()` waits until US-010.
  - Running Cmd still has no `--mode`.
- Review: no comments.

## 2026-08-30 13:34 CEST - scope override: US-015 in, one implementer

- User delivered LoL production model on this VPS (`data/lol/models/production/`, name `20260830T112804Z`, commit `13402bf` / merge `520deb8`). Contract on disk: `state_source=lolesports_window_details_grid_networth`, `feature_set=base`, horizon 300, `train_second_max=540`, `row_stride=1`, `train_matches=4229`, 12 canonical features.
- Explicit override: after US-014, run US-015 (do not leave it pending). Simplified flow like US-003: one deployment implementer via feature-json-implement-step, no separate planner or code-quality reviewer.
- User accepted this model for paper launch. Set `DOTA_TRADING_MODE=live`, `LOL_TRADING_MODE=paper`, recreate `trader-live` + `trader-paper` only at US-015.
- Continue sequential US-008..US-014 first. Do not recreate dota_2_model until US-015.

## 2026-08-30 13:36 CEST - US-008

- Replaced compose `live-paper` with `trader-live` (`--mode live`, `./data/live_paper_live`) and `trader-paper` (`--mode paper`, `./data/live_paper_paper`). Dropped `env_file`. Listed keys under `environment`. PK/BROWSER_ADDRESS only on live. Archives at `/archive/dota` and `/archive/lol`. No `ARCHIVE_ROOT`. Makefile `trader` with `MODE ?= paper`. Empty gitignored host dirs; did not copy `data/live_paper`. File-only: no compose up/restart.
- Checks: 8 compose tests + 36 with orchestrator/trading_mode; `make lint-all` passed; basedpyright 0 errors. `docker compose config --services` = trader-live, trader-paper. Paper env has no PK/BROWSER_ADDRESS/LIVE_TRADING. live-paper Id=`a188e0d8ebb5…` StartedAt=`2026-08-30T07:23:39Z` unchanged, no trader-* containers. Commit `2d6d2c9`.
- **Learnings for future iterations:**
  - `env_file: .env` dumps `PK` and `LIVE_TRADING` into every service. Drop it and list keys. Project `.env` still interpolates `${PK}` for live only.
  - `docker compose config` interpolates secrets; tests must use `--no-interpolate` and assert key names, never values.
  - Do not default `DOTA_TRADING_MODE` to live in YAML; empty is fail-closed until US-015 edits `.env`.
  - Running container is still `live-paper` / `./data/live_paper`. YAML rename does not recreate it. A stray `up -d` would start a second live wallet on empty `live_paper_live`.
  - `ARCHIVE_ROOT` removed because mounts moved under `/archive/dota` and `/archive/lol`. Accidental recreate before US-010 fail-closes on `load_archive_root`.
  - Image currently has no `/app/.env`, but a future `--build` without dockerignore can bake host `.env` and `env_value` would feed `PK` to paper.
- Review: no comments.

## 2026-08-30 13:51 CEST - US-009

- Required `game` on `DiscoveredMatch` / `MatchStart`. Discovery `_build_match(..., "dota")` is the US-010 seam. `match.json` schema 5 writes `game`; leftovers 3/4 stay unread.
- Kalshi helpers use "has kalshi object", not `schema_version == 4`, so schema 5 still binds. Boot scan ACCEPTED includes 5.
- Checks: 284 pytest passed; `make lint-all` passed; basedpyright 0 errors. live-paper Id=`a188e0d8ebb5…` StartedAt=`2026-08-30T07:23:39Z` unchanged. Commit `311493a`.
- **Learnings for future iterations:**
  - Do not inject `game` into parsed v3/v4 dicts (`finalize`/`pin_horn` rewrite the dict). Default only in `_match_start_from_document`.
  - `kalshi_reason_from_document` / `update_kalshi_binding` must not stay `== 4` or schema 5 kills Kalshi.
  - Session journal stays schema 6; no `game` on provenance.
  - `_build_match(..., "dota")` is the US-010 seam; do not rewrite MarketDiscovery this story.
  - `make lint-all` skips untracked — `git add` first.

## 2026-08-30 14:07 CEST - US-009 review

- Fixed nit: `MatchMetaKalshi` docstring now says schema 4+ (matches `read_kalshi_block` / `kalshi_reason_from_document`). No other findings.
- Checks: `make lint-all` passed; basedpyright 0 errors. Tests skipped (docstring only). live-paper Id/StartedAt unchanged. Commit `f8836ad`.
- US-009 stays passes true.

