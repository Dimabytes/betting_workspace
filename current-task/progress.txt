## 2026-08-28 00:50 - US-001

- Expanded LoL Stage 02 to download book_snapshot_full, trades, and onchain_fills in one resumable run. Capture root is still books_dir.parent. No --channels, no new downloader.
- Ready books skip HTTP; trades required, onchain_fills optional (404 / empty window / retries_exhausted stay audit gaps and do not fail the run). Audit gained channel; rebuilt each run.
- Fixture tests in tests/test_lol_fetch_telonex_books.py: 18 passed. make lint-all passed. make test passed (1531).
- Commit in dota_2_model: 9729d743e70465fb9425c873b85efa7db7b89a50
- **Learnings for future iterations:**
  - Keep --books-dir as the book channel directory; siblings trades/ and onchain_fills/ fall out of books_dir.parent.
  - Catalog windows are independent per channel; empty books still plans trades/onchain.
  - Do not import src/backtest/telonex_local.py into LoL Stage 02. Execution source selection is US-003.
  - make lint-all skips untracked files; git add first.
  - Parallel WIP on constants/lol.py (LOL_VALIDATION_START_TIME) was left uncommitted; do not fold it into Stage 02.

## 2026-08-28 01:05 - US-001

- Review fix: incomplete fails unless channel is in TELONEX_OPTIONAL_CHANNELS. Market miss channel="" fails with no special case. Dropped REQUIRED_CHANNELS alias.
- window_for has three named ifs then raise. download_one looks up TELONEX_CHANNEL_REQUIRED_COLUMNS; missing key is KeyError. write_channel raises on unknown channel.
- Skipped nits: no second test module, BookJob kept.
- tests/test_lol_fetch_telonex_books.py: 19 passed. make lint-all passed.
- Commit: f8e7a99a49de877988a072d9f83f604b29813386
- **Learnings for future iterations:**
  - Optional vs required is one membership test against TELONEX_OPTIONAL_CHANNELS, not a required list plus "".
  - Channel dispatch must not fall through to onchain_fills.

## 2026-08-28 01:10 - US-001 review

- Review findings fixed in f8e7a99a; nits skipped. US-001 passes.

## 2026-08-28 01:45 - US-002

- Added Stage 07 `src/lol/07_prepare_backtest.py`: offline signals 0..899, market seconds 0..1200, audit. Always rebuild. No network, no model predict.
- Parameterized `select_grid_rows(end_second)`. Stage 05 still requests 540 via `prepare_map_livestats` wrapper. Added `wall_us_for_second` and last_game_time/pauses on LivestatsOk.
- Rows 0..540 rebuilt with `join_market_rows` must equal validation.parquet or abort before publish. Inference 541..899 uses the same features + current mid, no 300s label.
- Two clocks: signal `state_ts_us` is selected frame wall; market-second `state_ts_us` is pause-adjusted second boundary. Cutoff at 1200 fails closed (`missing_cutoff_midpoint`) without last-stale-price reuse.
- Fixture tests in tests/test_lol_prepare_backtest.py: 11 passed. Stage 05 tests: 23 passed. make lint-all passed. make test passed (1543).
- Commit in dota_2_model: 67f9344908c18ebf507da976e6925d2f32d2435a
- Did not set feature.json passes. Did not run real data/lol Stage 07 (US-005).
- **Learnings for future iterations:**
  - Cannot `from lol.05_prepare_dataset import`; the `05` prefix is not a valid identifier. Use `import_module` + Protocol like the other stages.
  - Stage 07 eligibility uses UTC day stems on book parquets; Stage 05 `write_token_book` always writes `2026-01-01.parquet`. Tests need a dated writer. Spawn 1700000000 is 2023-11-14.
  - SPAWN_TS is before LOL_VALIDATION_START_TIME, so Stage 05 puts those maps in training. Tests copy production_training → validation and force split=validation.
  - Do not import backtest.context or backtest.telonex_local from src/lol. Duplicate the day-file check with telonex_root as an argument (US-003 will pass capture_root later).
  - Labeled 0..540 must use the Stage 05 0..540 grid (including age-gated seconds past last frame) so exact-match against validation.parquet does not abort early-end maps. Cap last_game_time only for inference 541..899 and market seconds 0..1200.
  - python modules named `07_*.py` import as `lol.07_prepare_backtest`.

## 2026-08-28 02:03 - US-002 review

- Labeled-match gate runs only when `join_market_rows` actually ran (`CardBuild.labeled_rebuilt`). Livestats drops and failed book/prior loads stay audit-only; sibling eligible cards still publish.
- Added `test_livestats_drop_stays_audit_when_sibling_matches`. 12 Stage 07 tests passed. Stage 05: 23 passed. make lint-all passed.
- Commit: 5de4c873f7cd27fc7d47f4846204365199b25b13
- Skipped cutoff `market_rows[-1]` nit.
- Did not set feature.json passes.

- **Learnings for future iterations:**
  - Empty labeled_rows vs nonempty validation.parquet is a global abort only after a real rebuild. A card that never joined must not take the sibling set down with it.

## 2026-08-28 01:40 - US-002 review

- Review finding fixed in 5de4c873 (labeled-match gate only after rebuild). Cutoff nit skipped. US-002 passes.

## 2026-08-28 03:10 - US-003

- Connected LoL to shared `src/backtest/run.py` via `--game dota|lol` (default dota). Two input functions: `load_dota_selection` in run.py, `load_lol_selection` in `lol_inputs.py`. No plugin/ABC/registry.
- Telonex helpers take required `capture_root`. Per token-day execution is nonempty `onchain_fills` else `trades` (file symlinks, not both dirs). `build_match_signals(..., lag_seconds)`; LoL passes 0.
- `make lol-backtest` adds `--game lol` + B0/S2. `make backtest` unchanged. LoL artifacts under `data/backtests/lol_maker`. Dota manifest keys unchanged.
- Tests: Dota CLI/Makefile regression, LoL dispatch/output root, source pick, lag gate, resume rejection, paired B0/S2 `build_decision`. pytest 1554 passed. make lint-all passed.
- Commit in dota_2_model: 1640692c9551399742d62c238180dfab4105ab1b
- Did not set feature.json passes. Did not implement US-004 cutoff/tail PnL. Did not run real `make lol-backtest` (US-005). Did not push.
- **Learnings for future iterations:**
  - Dota `--resume` fingerprints the current key set. Do not add `game`/`model_sha256` to Dota manifests.
  - `lol_inputs` must not import `backtest.run` (`test_backtest_imports`). `ReplayLookups` lives in `context.py`.
  - Stage 07 `state_ts_us` is already pause-adjusted; one synthetic OpenDota pause at time 0 aligns `buy_cutoff_ns` with second 540.
  - Manifest `cutoff_rule=stage07_replay_end` is a stub. US-004 may rename it when forced-hold accounting is real.

## 2026-08-28 03:25 - US-003 review

- Dropped `load_replay_lookups_for_game` (dual optionals + ValueError). After `if game == "lol"`, bind `load_lookups` once to nested `load_lol_lookups` / `load_dota_lookups`; three sites call `load_lookups(known_ids)`.
- Deleted `resolve_report_root`. `load_dota_selection` inlines `BACKTESTS_DIR / EXPERIMENT_NAME`. Dispatch test asserts `LolSelection.report_root` and `BACKTESTS_DIR / EXPERIMENT_NAME`.
- Skipped nits: `OPTIONAL_CHANNELS` in telonex_local.py; full `main()` dispatch in the test.
- Targeted pytest 93 passed. make lint-all passed. make test passed (1554).
- Commit: f8586b9b92653f4b34de81b72298706ee147bb8b
- Did not set feature.json passes. Did not push.
- **Learnings for future iterations:**
  - Nested closures with different names (`load_lol_lookups` vs `load_dota_lookups`) then `load_lookups = ...` avoids ruff F811 and keeps basedpyright happy on both branches.
  - LoL report root lives on `LolSelection`; do not add a second helper that Dota-only callers must remember to use.

## 2026-08-28 02:10 - US-003 review

- Review findings fixed in f8586b9b (drop game dispatcher and report_root helper). Nits skipped. US-003 passes.

## 2026-08-28 03:40 - US-004

- Cutoff/forced-hold tail PnL: LoL still-going maps freeze via existing `game_end_ns` (Stage 07 1200 wall). No second timer, no strategy subclass, no poly-maker edit. 1-min `replay_end` drain kept.
- `MarketContext.forced_hold` / `cutoff_ns`: Dota always false/0. LoL from audit `still_going_at_1200` + exact market-second 1200 `state_ts_us`.
- Postprocess `attach_cutoff_accounting` (called from `build_maker_match_results`): exact mid only, no as-of. Missing mid fail-closed (qty kept, PnL zeros, skipped in arm sums). Non-hold: `pnl_at_cutoff=engine_pnl`, `tail_pnl=0`.
- Wallet settle for forced-hold at `max(cutoff_ns, Gamma closed_at)`. Dota tape unchanged.
- Shared summary/report rows: forced-hold maps/shares, pnl at cutoff, tail pnl, settled pnl. Dota prints zeros/identity. LoL `cutoff_rule=hard_1200_forced_hold`.
- Tests: hard cutoff BUY/SELL+cancel latency, arithmetic, both tokens, early end, missing mid, wallet ts, summary/report, checkpoint defaults, LoL wiring. make test 1566 passed. make lint-all passed.
- Commit in dota_2_model: 66b08465ce23b8ac83bee6f0c915c092caf627ea
- Did not set feature.json passes. Did not push. Did not run real make lol-backtest (US-005).
- **Learnings for future iterations:**
  - Do not infer `forced_hold` from duration; postprocess Dota fixtures use GAME_END = HORN+2000s and must stay non-hold.
  - Cutoff mid must use exact `state_ts_us * NS_PER_US`, not rounded `game_ended_at`.
  - Old Dota results.parquet resume needs missing-column defaults; parquet NaN mids must become None.
  - SELL-at-1200 tests need `predicted_delta=0.0` or unwind; default +5¢ fair blocks join-ask SELL.

## 2026-08-28 03:30 - US-004 review

- Review: NO COMMENTS. US-004 passes.

## 2026-08-28 08:40 - US-005 interrupted

- Mac kernel panic: WindowServer watchdog, compressor 100% (61 swapfiles). 9 parallel Nautilus shards + download. No summary.json.
- Stage 07 survived: 1513 cards, eligible 1408, missing_cutoff_midpoint 104, missing_required_channel 1.
- Production model exists. Trades on disk: 7472. onchain_fills: 0. Audit on disk is still the old book-only schema (Stage 02 did not finish writing the channel audit).
- Uncommitted run blockers kept: Int64 nw_delta NaN, subsecond cutoff pause rounding, Stage 02 httpx GET (stream pool leak), dotenv in main.
- Resume research backtest with **3** parallel shards, not 9, so this machine does not panic again.




