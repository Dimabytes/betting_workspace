US-005 plan written: current-task/plans/US-005.md
Scope: VPS bootstrap runbook (agent rsync phase via `ssh sun` root/BatchMode: local `inventory` on both Mac onchain_fills trees → `--files-from` rsync of inventoried closed files only → `chown -R 10001` → operator phase after `git pull`: `docker compose build archive`, `.env` `ONCHAIN_RPC_URL_A/B` ← `ALCHEMY_POL_ENDPOINT`/`_RESERVE` by hand, `accept --dry-run` → real `accept` → `ONCHAIN_START_DATE` from report → `up -d onchain`; import runs as image UID 10001, lock ordering accept-before-up), `sync_collector_parquet.py` manifest-driven onchain phase (fetch `<archive>/manifests/onchain/` → ready+date<today filter → install/kept/replace/conflict vs local sha256 → `--files-from` pull into in-dest staging → verify sha256 → os.replace; `--onchain-start-date` gates replace; today stays excluded; books rsync gains `--exclude=onchain_fills/`), `telonex_onchain.py` split into `shared/utils/telonex_capture.py` (asset_channel_dir + FatalDownloadError/validate_target/write_arrow_table/download_url/retry_delay; 5 one-line import swaps), deletions (2 scripts, 2 tests, module, Makefile targets+phony).
Key decisions: script stays stdlib+msgspec+rsync_pull only (viewer spawns it without PYTHONPATH — no `shared.*` imports); `rsync_pull.run_rsync_status` added so a missing remote `manifests/onchain/` warns and skips; verify-then-install staging = TemporaryDirectory inside dest (same-fs os.replace); manifests parsed via msgspec Structs with strict path/assetId/date sanity; `dest_parquet`+ONCHAIN_SCHEMA+planner die with the module (schema is collector-owned now).
Operator decision flagged (§2 of plan): 31,092 older 20-column legacy files are `skipped` at inventory — default documented exclusion vs optional Mac-side normalize-to-26-col pass vs rejected collector loosening; decision precedes the rsync.
Verified now: ssh sun BatchMode as root works; no `/var/lib/polymarket-onchain-state` yet, 248 GiB free, no `manifests/onchain/` yet; openrsync has `--files-from`/`--partial`; fixture move already done in US-001/002 (deleted tests are fully synthetic); dirty worktree incl. `telonex_local.py` is parallel work — stage only US-005 paths, no resets.
Tests: new pure-function cases in test_sync_collector_parquet.py; targeted pytest (sync/fetch-books/tape/map-cache) + import smoke + `make lint`; no full suite during a live map; collector untouched unless a gap appears (then yarn check green).
Gate: two commits max — esports-trader `feat: [US-005] - VPS enable and delete the Python on-chain collector`; no git push, no `docker compose down`, VPS `onchain` start is operator-only.
