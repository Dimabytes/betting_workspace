US-003 plan written: current-task/plans/US-003.md
Scope: planner over ONCHAIN_START_DATE days under one pinned catalog fingerprint; durable append-only per-day JSONL ledger of chunk commits (bounds, addresses, decoder version, fingerprint, control blocks, finalized head — incl. empty ranges); unlessCommitted-gated chunk scans; staged .onchain/ writes + atomic rename to parquet/onchain_fills/asset_id=<token>/<date>.parquet; per-game manifests/onchain/<date>.json published last; day complete only when both games ready.
New files: src/onchain-plan.ts, src/onchain-day.ts, src/onchain-publish.ts, src/onchain-validate.ts, src/schema/onchain-manifest.ts + 6 test files. Changed: onchain-ledger.ts (durable impl; service shape and markCommitted(range) kept per US-002 contract), onchain-config.ts (startDate/roots/stateDir/chunkBlocks), versions.ts, schema/onchain.ts (row wire codec), onchain.testing.ts.
Key decisions: isCommitted scoped by (date, range, fingerprint, decoderVersion) so catalog/decoder changes rescan automatically; coverage = per-chunk union over snapshot token sets — a new token marks only its pair uncovered, ready files of other tokens stay; no global cursor (completeness derived per day); manifest readiness compares covered token ids, not fingerprint equality.
Crash tests: kill before commit (RPC fails mid-scan → refetch only uncommitted), after commit (wrapped markCommitted fails post-append → committed chunks not refetched, ledger intact), between game publishes (second run: zero getLogs, first game's manifest untouched, second publishes).
Verified now: publishJsonFile/publishBytesAtomic, sha256Canonical/sha256File, sameTypeTree/queryRows, openDuckDbSession(.onchain), manifests/ never enumerated so manifests/onchain/<date>.json cannot collide; manifest.test.ts already pins onchain_fills rejection; chainResponder/makeRow promoted to onchain.testing.ts.
Non-goals explicit: onchain-main, compose service, lock/cadence/SIGTERM/Telegram/disk, import CLI, live Alchemy, VPS, esports-trader — all US-004/005.
Gate: yarn check green; no new deps; poly-maker untouched.
