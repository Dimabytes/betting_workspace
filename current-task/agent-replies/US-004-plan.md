US-004 plan written: current-task/plans/US-004.md
Scope: fifth compose service `onchain` (image + `command: ["node","dist/onchain-main.js"]`, both archive binds + `/onchain-state`, no POLYMARKET_TAG_ID, Alchemy URLs + ONCHAIN_START_DATE via env interpolation `:-`), `src/onchain-main.ts`/`onchain-app.ts` process (`.onchain.lock` on stateDir, startup `.onchain/` sweep keeping unfinished chunks, disk gate via DiskProbe+diskLevelOf+Telegram transitions, 5-min pass loop + `ONCHAIN_RUN_ONCE`, SIGTERM via runMain), pass report (today/lastFullDay/lag/remainingDays+uncovered/outcomes/fills-per-game/endpoint state), and `onchain-import.ts` + `onchain-import-main.js` CLI (inventory of closed parquet only; hash/schema/asset_id checks; statuses legacy_imported/legacy_empty_unverified/full_day; bootstrap report writes onchainStartDate).
New files: onchain-main.ts, onchain-app.ts, onchain-import.ts, onchain-import-main.ts + 2 test files. Changed: compose.yaml, docker.test.ts (counts 4→5, binds 4→7, onchain block asserts), onchain-config.ts (disk thresholds, runOnce, empty-URL reject, OnchainDuckDbLive), onchain-rpc.ts (requests/bytesIn/retries Refs, readEnvelope via arrayBuffer for byte counts), onchain-day.ts (runOnchainPass → {plans,outcomes}; DayOutcome += committed/totalChunks), fs/layout.ts, telegram.ts, .env.example, docs/docker-compose.md, learnings.
Key decisions: DuckDbSettingsLive derives from Settings (needs POLYMARKET_TAG_ID) — onchain gets its own layer; openDiskMonitor needs a Journal so the disk gate reuses DiskProbe+diskLevelOf directly (stop → skip pass, write failures never mark coverage since manifests land last); `full_day` is ONLY a ready manifests/onchain/<date>.json covering the token — non-empty legacy never earns it, empty files are legacy_empty_unverified; import writes no manifests/onchain entries.
Verified now: ssh sun works BatchMode (sidecars rsyncable: 979+1087 markets); Alchemy pair exists in esports-trader/.env; Mac legacy trees 116k files/5.8GB; compactor sweeps (.compaction/, parquet/**/*.tmp) already can't touch .onchain/ — pinned by test, no code change; publishBytesAtomic temps are compactor-safe inside parquet/.
Live run: rsync real sidecars to /tmp roots, yarn build, one ONCHAIN_RUN_ONCE pass for a chosen completed day, PyArrow ONCHAIN_SCHEMA check from esports-trader, record time/requests/bytes/CU — no speed claim. Import dry-run over real Mac trees (source doubles as staging).
Non-goals explicit: VPS enable/bootstrap, esports-trader + sync changes, Python deletion, poly-maker — all US-005.
Gate: yarn check green; no new deps; commit `feat: [US-004] - Compose onchain service, import CLI, local live Alchemy day`.
