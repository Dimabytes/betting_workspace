---
description: After every backtest, print the two-column terminal report. Never dump summary.json.
alwaysApply: false
---

# Backtest terminal report

After a backtest finishes, or when the user asks to show backtest / S2 results, print `format_terminal_report` from `summary.json` verbatim.

Never paste JSON. Never use markdown, bold, or markdown tables.
