# US-001: Freeze the extraction oracle and disputed scenarios

## Goal

Pin Follow300 extraction identity on current `ladder-experiment` so later steps can tell preserved behavior from labeled fixes. Do not implement US-002+ and do not change the four later-labeled behaviors.

## In-scope identity

Fields: fills, place/cancel, episode/level links, reserve size and timestamps.

Maps (seed 0): Dota `8837869969`, `8911784562`, `8933879286`; LoL `115564793879469302`.

Compact harness scenarios: partial BUY fill; rung move that keeps queue; fill/cancel race.

PnL is a check field on the map goldens, not the only gate. `no_quote` rows are out of identity (cadence noise). Known Nautilus zero-fill maps stay labeled engine faults and are not these four ids.

## Later labeled fixes (document only)

- Late old-episode fill (`_credit_detached_level_fill` matches rung index, ignores `episode_id`)
- SELL min-life 1s + MATCHED-unconfirmed hold (live table; backtest extraction is 10s restart + cancel working SELL)
- Recalc cadence 100ms debounce + 2s fallback (extraction keeps 1s timer)

## Plan

1. Re-run the four maps on current HEAD (`python src/backtest/run.py --match-id … --signal-cadence-seed 0 --name extraction-oracle`). Capture code SHA, policy/config, model SHA, input paths into `docs/extraction-oracle.md`.
2. Project identity fields from each run dir into small JSON goldens under `tests/fixtures/extraction_oracle/`. Do not check in parquet dumps.
3. Add `src/backtest/extraction_identity.py`: frozen identity dataclasses, project from `FillRecord`/`QuoteEvent`, load/dump JSON, compare.
4. Compact goldens from the existing maker harness (not Nautilus): partial fill, reattach-keep-queue, fill during cancel.
5. Tests load map goldens (schema + match ids + identity keys) and replay the three harness scenarios against compact goldens. No full-map pytest.

## Out

poly-maker. Core `src/strategy/`. Cadence/SELL/late-fill code changes. Betting_workspace commit (orchestrator owns it).
