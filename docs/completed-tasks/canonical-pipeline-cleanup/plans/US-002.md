# US-002 — Remove LoL forced-hold accounting

> **For the implementer (`feature-json-implement-step`):** implement **US-002 only** (all three source-plan Tasks). Do **not** start US-003+. Do **not** edit `../poly-maker`. Do **not** run full prepare/train/backtest jobs. Do **not** set `passes: true` in `feature.json` — the orchestrator does that after review. Work on `main`. Prefer the source plan’s **three commits**. One commit is allowed if cleaner; if you squash, skip Task 1’s transitional `forced_hold=False, cutoff_ns=0` and delete those `MarketContext` fields in the same change.

**Story:** `current-task/feature.json` → `US-002` (`passes: false`).
**FR:** FR-2 — LoL replay, results, wallet, and summaries use the last livestats frame and engine PnL; no forced-hold or cutoff accounting.
**Spec:** `/Users/dimabytes/work/polymarket/dota_2_bot/dota_2_model/docs/superpowers/plans/2026-09-03-remove-lol-forced-hold.md` (source plan; follow its exact reductions).
**Product repo:** `/Users/dimabytes/work/polymarket/dota_2_bot/dota_2_model` on `main`.
**Workspace:** `/Users/dimabytes/work/polymarket/dota_2_bot/betting_workspace`.
**US-001 is done** (`e6aae12`). Four-shard launcher is on main. Do not redo it.

**Commits (dota_2_model), prefer this split:**

1. `refactor: use final LoL frames because every fetched map reaches map end`
2. `refactor: drop forced-hold accounting because no LoL map reaches the safety cap`
3. `docs: record final-frame settlement after zero forced-hold maps`

---

## 0. Constraints (hard)

- `../poly-maker` is frozen. Do not edit it.
- Preserve unrelated `dota_2_model` working-tree dirt. Today that is:
  - `D .streamlit/config.toml` (deletion)
  - `M docs/next_steps.md`
  - untracked `docs/superpowers/plans/2026-09-03-collapse-canonical-pipeline-optionality.md`
  Do **not** stage any of those. Re-check `git status` before every `git add`.
- Stage `docs/superpowers/plans/2026-09-03-remove-lol-forced-hold.md` with **Task 3 only**. Do **not** stage the collapse-canonical plan.
- Do not run `make prepare`, `make train`, `make backtest`, `make lol-backtest`, or `scripts/run_seeds.sh` with a real `<name>`.
- After any Python edit: `make lint-all`. `make lint-all` is `pre-commit run --all-files` and skips untracked files — `git add` touched paths then re-run.
- Git operations only inside the repo being changed. Product commits in `dota_2_model`. Workspace commit for this plan + `progress.txt` after the product commits are green.
- Keep `game_end_ns`, `_on_game_end`, and game-end cancel tests. Keep the 7200 fetch/prepare safety cap with `LOL_PREPARE_END_SECOND = LOL_FETCH_END_SECOND` (do not repeat `7200`). Keep training/production rows at seconds `0..540`. Keep Dota/LoL `onchain_fills` vs `trades`.
- `read_results_checkpoint` ignores extra legacy columns by selecting current dataclass fields. **No compatibility-fill branches.** Delete `_fill_cutoff_checkpoint_columns`.
- `buy_cutoff_ns`, `buy_cutoff_second`, `GateSeconds.cutoff`, `synthetic_cutoff_pauses`, and `_on_buy_cutoff` are the ordinary BUY-window cut. They are **not** forced-hold. Do not delete them. Task 3 `rg` does not match them.
- Keep `forced_liquidation` (unrelated stub).
- Do not edit historical experiment docs (`docs/experiments/lol-no-unwind.md`, `docs/no-unwind-default-plan.md`, `docs/experiments/sell-through-ask/`).
- Ponytail: shortest working diff. Follow the source plan’s exact reductions. No shims, no leftover fill-defaults, no extra helpers.
- Do **not** set `userStories[id=US-002].passes` to `true`.
- Do not commit broken code. Do not `--amend`. Do not push. Do not `--no-verify`.

**Feature.json vs source plan:** `changes[]` matches the source plan. Implement all three Tasks. “Add or update automated tests” = the source plan’s test edits (schema assertions, `test_context_ends_at_final_livestats_frame`, `test_read_results_checkpoint_ignores_legacy_cutoff_columns`, deleted forced-hold tests), not a new test module.

---

## 1. Read first (verified 2026-09-03 against the tree)

Read these before editing. They are the source of truth, not this section’s paraphrase.

| Path | Why |
|---|---|
| `docs/superpowers/plans/2026-09-03-remove-lol-forced-hold.md` | Spec. Tasks 1–3. |
| `src/shared/constants/lol.py` | `LOL_FETCH_END_SECOND` L31; `LOL_MARKET_END_SECOND` L259; `LOL_PREPARE_END_SECOND = LOL_MARKET_END_SECOND` L262; `REASON_MISSING_CUTOFF_MIDPOINT` L270, L290, L313. |
| `src/shared/types/lol.py` L243–268 | `LolBacktestAuditRow` still has `cutoff_ts` L258 and `still_going` L265. |
| `src/lol/replay.py` | `ReplayBounds` L45–54 (`cutoff_wall`, `still_going`); `replay_bounds` L123–146; `audit_reason` L215–229 (5 args). `book_load_window` L154–172 uses `last_market_second` + `wall_us_for_second` — keep. |
| `src/lol/05_prepare_dataset.py` | `BACKTEST_AUDIT_INTEGER_COLUMNS` includes `"cutoff_ts"` L152; cutoff_ok / audit cells L539–568. |
| `src/backtest/lol_inputs.py` | `build_lol_market_context` L120–164 still takes `forced_hold`/`cutoff_ns`; `load_lol_replay_lookups` L187–199 reads `still_going` and searches `LOL_MARKET_END_SECOND`. |
| `scripts/measure_lol_source_lag.py` L26, L115 | Validates through `LOL_MARKET_END_SECOND`. |
| `tests/test_lol_prepare_backtest.py` L179–226 | `still_going is False` on the 1500s and early-end tests. |
| `tests/test_lol_backtest.py` L147–185, L475–476, L532, L598–638 | Fixture `still_going`/`cutoff_ts`; `MarketContext` kwargs; `cutoff_rule == "map_end"`; two forced-hold tests. |
| `src/backtest/context.py` L90–91 | `MarketContext.forced_hold` / `cutoff_ns`. |
| `src/backtest/selection.py` L263–264 | Dota always `forced_hold=False, cutoff_ns=0`. |
| `src/backtest/marks.py` L49–55 | `lookup_exact_mid` — cutoff-only; delete in Task 2. Keep `lookup_reference_mid`. |
| `src/backtest/results.py` | `MakerMatchResult` L88–95 (eight cutoff fields); `build_maker_match_result` seeds them L233–240; `attach_cutoff_accounting` L244–315; `build_maker_match_results(..., mids=)` L318–353; `_optional_mid` / `_fill_cutoff_checkpoint_columns` / `read_results_checkpoint` L366–433. |
| `src/backtest/run.py` L147, L603, L754 | `LOL_CUTOFF_RULE`; LoL manifest `cutoff_rule`; `mids=mids` **only** on `build_maker_match_results`. Other `mids=` stay (enrich, summary, wallet). |
| `src/backtest/wallet_path.py` L198–206 | Forced-hold settle at `max(cutoff_ns, closed_at)`; else `game_ended_at`. |
| `src/backtest/postprocess.py` L82–85, L566–621 | ASSUMPTIONS forced-hold paragraph; five summary keys. |
| `src/backtest/report.py` L135–139 | Five forced-hold / cutoff PnL rows. |
| `src/shared/types/backtest.py` L137–141 | `ArmSummary` five matching fields. Keep `gate_cutoff_seconds` L122. |
| `tests/test_backtest.py` L94–95, L290 | Context kwargs; Dota `"cutoff_rule" not in manifest` — keep that negative assert. |
| `tests/test_backtest_validation.py` L156–163, L429–430, L523+, L763–788 | Result/context builders; `mids={}` on `build_maker_match_results`; old checkpoint-fill test. |
| `tests/test_backtest_postprocess.py` L70–71, L204–211, L340–453, L963–end | Builders + six forced-hold tests to delete. Ordinary summary is `test_summarize_arm_chain_excludes_terminated_and_reports_stub`. |
| `AGENTS.md` L180–195 | Forced-hold paragraph. Four-shard launcher (L128–131, L177, L198) already done — do not touch. |
| `docs/domain.md` L32 | One long bullet; forced-hold sentences at the end. L31 already four-shard — do not touch. |
| `current-task/feature.json` US-002 | Acceptance list. |
| `current-task/progress.txt` | US-001 passed; pending US-002. |

`tests/test_lol_prepare_dataset.py` has **no** forced-hold hits. Source-plan `git add` lists it; only add it if you actually edit it.

### Tree vs source-plan line numbers

Source plan named `lol.py:25-35`, `lol.py:255-315`, `types/lol.py:243-268`, `replay.py:45-55/123-146/215-229`, `05_prepare_dataset.py:480-575`, `lol_inputs.py:120-205`, `measure_lol_source_lag.py:20-120`, tests as listed, then Task 2 ranges, `AGENTS.md:180-196`, `docs/domain.md:32`.

| Claim | Today |
|---|---|
| `constants/lol.py:25-35` | Fetch cap **L31** `LOL_FETCH_END_SECOND = 7200` — **keep, no edit**. MARKET_END / PREPARE_END live at **L259–262**. |
| `constants/lol.py:255-315` | Exact enough. `LOL_MARKET_END_SECOND` L259; alias L262; reason L270; drop keys L290; count keys L313. |
| `types/lol.py:243-268` | Exact. Fields to drop: L258 `cutoff_ts`, L265 `still_going`. |
| `replay.py:45-55` | `ReplayBounds` is **L45–54**. |
| `replay.py:123-146` | Exact. |
| `replay.py:215-229` | Exact. |
| `05_prepare_dataset.py:480-575` | `LOL_PREPARE_END_SECOND` already passed at L481 (keep). Cutoff cells **L539–568**. Also delete `"cutoff_ts"` from `BACKTEST_AUDIT_INTEGER_COLUMNS` **L152** (plan did not name this list; the TypedDict drop requires it). |
| `lol_inputs.py:120-205` | Function starts L120; still_going branch **L187–199**. Task 2 later edits L145–165 (`MarketContext` kwargs). |
| `measure_lol_source_lag.py:20-120` | Import L26; call L115. |
| `test_lol_prepare_backtest.py:170-230` | 1500s test **L179–203**; early-end **L206–226**. |
| `test_lol_backtest.py:145-185` | Fixture **L147–185**. |
| `test_lol_backtest.py:598-640` | Two tests **L598–638**. File ends at L638. |
| `context.py:73-92` | Dataclass L73; fields **L90–91**. |
| `selection.py:250-266` | Kwargs **L263–264**. |
| `marks.py:45-56` | `lookup_exact_mid` **L49–55**. |
| `results.py:1-100` | Eight fields **L88–95**. |
| `results.py:180-355` | Constructor seed L233–240; `attach_cutoff_accounting` L244–315; `mids` kwarg L327 / return L353. |
| `results.py:365-435` | Fill-compat **L366–433**. |
| `run.py:140-150` | `LOL_CUTOFF_RULE` **L147**. |
| `run.py:580-608` | `"cutoff_rule": LOL_CUTOFF_RULE` **L603**. |
| `run.py:735-760` | `mids=mids` on results **L754**. L780+ are summary — keep. |
| `wallet_path.py:185-210` | Forced-hold branch **L202–205**. |
| `postprocess.py:55-90` | ASSUMPTIONS paragraph **L82–85**. |
| `postprocess.py:550-625` | Aggregation **L566–573**; keys **L615–621**. |
| `report.py:126-145` | Five rows **L135–139**. |
| `types/backtest.py:90-145` | Five fields **L137–141**. `gate_cutoff_seconds` L122 stays. |
| `AGENTS.md:180-196` | Forced-hold paragraph is **L180–195**. L196 is blank. Four-shard examples L177/L198 already fixed. |
| `docs/domain.md:32` | Exact. One long bullet. |

HEAD is `e6aae12` (US-001). Dirt: streamlit deletion, `docs/next_steps.md`, two untracked 2026-09-03 plans.

---

## 2. Current vs target

### 2.1 Task 1 — final-frame replay bounds

**`ReplayBounds` / `replay_bounds`:** copy the source-plan snippet. Drop `cutoff_wall` and `still_going`. Always `game_ended_at_wall = livestats.last_frame_wall_seconds`, `last_market_second = math.floor(livestats.last_game_time)`. Keep `math` (still used). Remove `LOL_MARKET_END_SECOND` import. Keep `wall_us_for_second` (book/market-second windows).

**Constants:** delete `LOL_MARKET_END_SECOND`. Set `LOL_PREPARE_END_SECOND = LOL_FETCH_END_SECOND`. Rewrite the comment so 7200 is the fetch/prepare safety cap, not “still-going”. Delete `REASON_MISSING_CUTOFF_MIDPOINT` and both list entries (L290, L313).

**`audit_reason`:** copy the source-plan 3-arg function. Drop `still_going` / `cutoff_ok` / `REASON_MISSING_CUTOFF_MIDPOINT`.

**`LolBacktestAuditRow`:** delete `cutoff_ts` and `still_going`. Keep `game_ended_at_ts`.

**`05_prepare_dataset.py`:** delete `cutoff_ok` block (L539–542), `cutoff_ts = ...` (L547), audit keys `"cutoff_ts"` and `"still_going"`. Call `audit_reason(has_books, has_trades, len(joined.rows))`. Remove `"cutoff_ts"` from `BACKTEST_AUDIT_INTEGER_COLUMNS` (L152). `BACKTEST_AUDIT_COLUMNS` is derived from annotations — it shrinks with the TypedDict.

**`lol_inputs.py` (Task 1 only):** drop `LOL_MARKET_END_SECOND` import. Remove `forced_hold`/`cutoff_ns` **parameters** from `build_lol_market_context`. Delete L187–197 (`still_going` + 7200 search). Call `build_lol_market_context(audit_row, gamma_markets, horn_at=horn_at)`. Until Task 2, the `MarketContext(...)` call still passes:

```python
forced_hold=False,
cutoff_ns=0,
```

**`measure_lol_source_lag.py`:** import and pass `LOL_PREPARE_END_SECOND` instead of `LOL_MARKET_END_SECOND`.

**Tests Task 1:**

- `test_map_past_prepare_end_stops_at_last_frame`: bind `last_wall = float(SPAWN_TS + 1500)` (same pattern as early-end’s `SPAWN_TS + 100`). Replace `assert bool(audit["still_going"]) is False` with the source-plan trio (`still_going` / `cutoff_ts` not in index; `game_ended_at_ts == round(last_wall)`). Keep the “does not extend to 7200” asserts (`LOL_PREPARE_END_SECOND not in …`, `1501 not in market_seconds`).
- `test_early_game_end`: same trio using the existing `last_wall`. Keep replay_end / eligible asserts.
- `audit_row` fixture: remove `still_going` param and key. Also drop `"cutoff_ts"` (schema no longer has it). Leave `game_ended_at_ts`.
- Delete `test_still_going_audit_sets_forced_hold_cutoff_ns`.
- Replace `test_early_end_audit_is_not_forced_hold` with `test_context_ends_at_final_livestats_frame` asserting `context.game_ended_at.timestamp() == GAME_END_TS` for every context. Drop `LOL_MARKET_END_SECOND` import if unused.
- Leave `forced_hold=False, cutoff_ns=0` on the Telonex `MarketContext` in this file until Task 2. Leave `assert expected["cutoff_rule"] == "map_end"` until Task 2.

### 2.2 Task 2 — delete generic forced-hold accounting

**`MarketContext`:** delete `forced_hold` and `cutoff_ns`. Remove those two kwargs from:

- `src/backtest/selection.py` (L263–264)
- `src/backtest/lol_inputs.py` (the Task 1 transitional pair)
- `tests/test_backtest.py` L94–95
- `tests/test_lol_backtest.py` L475–476
- `tests/test_backtest_validation.py` `build_context_for_results` L429–430
- `tests/test_backtest_postprocess.py` `build_context` L70–71

**`MakerMatchResult`:** delete the eight fields. End `build_maker_match_result` at `stop_reason=stop_reason` — copy the source-plan constructor (it matches today’s fields minus those eight). Remove the L233–240 seed kwargs.

Delete `attach_cutoff_accounting`, `_optional_mid`, `_fill_cutoff_checkpoint_columns`. Delete `lookup_exact_mid` from `marks.py`. From `results.py` drop now-unused `replace`, `isnan`, `MidSeries`, `lookup_exact_mid`, `settlement_value_for_token`, `token_mid`. Keep `MarketContext` (still used by `build_maker_match_result`).

**`build_maker_match_results`:** drop the `mids` keyword argument; `return` the list comprehension directly (no `attach_cutoff_accounting`). In `run.py` remove **only** `mids=mids` from that call (L754). Keep `mids` for enrich_fills, markouts, drawdown, wallet MTM, `write_finished_summary`.

In `tests/test_backtest_validation.py` drop `mids={}` from every `build_maker_match_results(...)` call (today: L523, L585, L620, L647, L668, L964). Do **not** touch `mids={}` on `build_summary_payload` (L711) or `replay_matches` (L1189).

**`read_results_checkpoint`:** copy the source-plan function **verbatim**. Selecting `frame[field_names]` ignores extra columns. No filler, no `_optional_mid`.

Replace `test_read_results_checkpoint_defaults_old_dota_columns` with `test_read_results_checkpoint_ignores_legacy_cutoff_columns`: write an otherwise **current** row **plus** the eight legacy columns, then `assert loaded[0] == result` (current dataclass, extras ignored). Invert of today’s test (today *drops* columns and fills defaults — that path is deleted).

**Wallet:** in `_build_tape_events`, remove `contexts` from the settle branch and always `settle_ns = datetime_to_ns(parse_utc(result.game_ended_at))`. Keep the public `calculate_wallet_path(..., contexts)` argument — `_Tape` still needs context for midpoint / binary settlement.

**Summary / report / types:** delete the forced-hold paragraph from `ASSUMPTIONS` (L82–85). Delete `forced_hold_rows` aggregation (L566–573) and the five summary keys (L615–621). Delete the five `_format_kv` rows in `report.py`. Delete the five `ArmSummary` fields (L137–141). Do not touch `total_engine_pnl`, `pnl_before_rebate`, `net_pnl`, markouts, drawdown, terminal inventory, or `forced_liquidation`.

**Manifest:** delete `LOL_CUTOFF_RULE` (run.py L147) and `"cutoff_rule": LOL_CUTOFF_RULE` (L603). In `test_lol_backtest.py` delete `assert expected["cutoff_rule"] == "map_end"`. Keep Dota `assert "cutoff_rule" not in manifest` in `test_backtest.py` L290.

**Delete these tests in `test_backtest_postprocess.py` (L963–end):**

- `test_lookup_exact_mid_matches_only_that_timestamp`
- `test_cutoff_tail_settled_arithmetic`
- `test_both_token_inventories_sum`
- `test_early_game_end_is_not_forced_hold`
- `test_missing_cutoff_mid_does_not_use_stale_asof`
- `test_forced_hold_wallet_settles_at_max_cutoff_and_closed_at`
- `test_summary_and_report_forced_hold_fields`

`lookup_reference_mid` already has `test_as_of_falls_back_to_last_fresh_mid` — do not keep a leftover exact-mid assert. Drop `lookup_exact_mid` and `attach_cutoff_accounting` imports. Keep `from dataclasses import replace` (`test_min_equity_*` still uses it).

**Add** the source-plan `removed` disjoint assert to `test_summarize_arm_chain_excludes_terminated_and_reports_stub` (after the existing summary asserts, ~L453):

```python
removed = {
    "forced_hold_maps",
    "forced_hold_shares",
    "pnl_at_cutoff",
    "tail_pnl",
    "settled_pnl",
}
assert removed.isdisjoint(summary)
```

Strip the eight constructor kwargs from `build_result` (`test_backtest_validation.py` L156–163) and `build_result_row` (`test_backtest_postprocess.py` L204–211).

### 2.3 Task 3 — docs

**`AGENTS.md` L180–195** (the LoL runner paragraph). Keep `--game` default, shared runner/strategy, unwind, sharding, resume, `TELONEX_DISABLE_POLYMARKET_TRADE_FALLBACK=1`, `onchain_fills` vs `trades`, `lag_seconds=0`, `data/backtests/lol_maker/`, Stage 04 fetch-until-404, 7200/10800 safety caps, quoting through the last frame, `game_end_ns` cancel, train/production `0..540`. Delete still-going / hold leftover / `pnl_at_cutoff` / `tail_pnl` / Gamma-close settle / `forced_hold=False` / `cutoff_rule=map_end`. State: Stage 05 grids through the last fetched frame; leftover inventory settles on the normal engine game-end path; result and summary artifacts expose the single `engine_pnl` path.

Do **not** rewrite L128–131 / L177 / L198 (US-001). Do not rewrite L18 (already “last frame (7200s safety cap)”).

**`docs/domain.md` L32** — same facts. Keep the onchain_fills/trades sentence, `lag_seconds=0`, Stage 04 fetch+details, `--workers`. Replace the forced-hold tail of the bullet (from “Maps still going at second 7200…” through `cutoff_rule=map_end`) with final-frame `game_end_ns` cancel + ordinary engine settlement + single engine PnL. Leave L31 (four-shard) and L34 (7200s safety cap on validation/market seconds) alone.

Leave `docs/experiments/lol-no-unwind.md` (`forced_hold` 0 is historical) and `docs/no-unwind-default-plan.md` unchanged. Leave `docs/experiments/sell-through-ask/` (outside Task 3 `rg`).

---

## 3. Ordered steps (source plan Tasks 1–3)

Work in `/Users/dimabytes/work/polymarket/dota_2_bot/dota_2_model` on `main`.

### Task 1 — final-frame replay

#### T1.1 — Red tests

Edit `tests/test_lol_prepare_backtest.py` as in §2.1. Then:

```bash
uv run pytest tests/test_lol_prepare_backtest.py::test_map_past_prepare_end_stops_at_last_frame tests/test_lol_prepare_backtest.py::test_early_game_end -q
```

Expected: **FAIL** (`still_going` / `cutoff_ts` still on audit rows).

#### T1.2 — Production reductions

Apply §2.1 to constants, types, `replay.py`, `05_prepare_dataset.py`, `lol_inputs.py`, `measure_lol_source_lag.py`, and the `test_lol_backtest.py` fixture/tests.

#### T1.3 — Focused LoL tests

```bash
uv run pytest tests/test_lol_prepare_backtest.py tests/test_lol_backtest.py tests/test_lol_prepare_dataset.py -q
```

Expected: PASS. No generated `still_going` / `cutoff_ts`. Every LoL context ends at `game_ended_at_ts`.

#### T1.4 — Lint then commit

```bash
git add src/shared/constants/lol.py src/shared/types/lol.py src/lol/replay.py src/lol/05_prepare_dataset.py src/backtest/lol_inputs.py scripts/measure_lol_source_lag.py tests/test_lol_prepare_backtest.py tests/test_lol_backtest.py
make lint-all
```

Only add `tests/test_lol_prepare_dataset.py` if you edited it. Confirm dirt still unstaged.

```bash
git commit -m "$(cat <<'EOF'
refactor: use final LoL frames because every fetched map reaches map end

EOF
)"
```

### Task 2 — accounting deletion

#### T2.1 — Red tests

Edit summary/result tests as in §2.2 (delete the seven postprocess tests; add `removed.isdisjoint`; replace checkpoint test; strip constructor kwargs). Then:

```bash
uv run pytest tests/test_backtest_validation.py tests/test_backtest_postprocess.py tests/test_lol_backtest.py -q
```

Expected: **FAIL** (dataclasses / summaries still require or emit forced-hold fields).

#### T2.2 — Production reductions

Apply §2.2. Copy `read_results_checkpoint` from the source plan. Copy `MakerMatchResult(...)` from the source plan. Transitional `forced_hold=False, cutoff_ns=0` in `lol_inputs.py` goes away with the fields.

#### T2.3 — Backtest unit suites

```bash
uv run pytest tests/test_backtest.py tests/test_lol_backtest.py tests/test_backtest_validation.py tests/test_backtest_postprocess.py tests/test_backtest_maker.py -q
```

Expected: PASS. `test_backtest_maker.py` is **unchanged** (game-end cancel / `buy_cutoff_ns` stay).

#### T2.4 — Lint then commit

```bash
git add src/backtest/context.py src/backtest/selection.py src/backtest/lol_inputs.py src/backtest/marks.py src/backtest/results.py src/backtest/run.py src/backtest/wallet_path.py src/backtest/postprocess.py src/backtest/report.py src/shared/types/backtest.py tests/test_backtest.py tests/test_backtest_validation.py tests/test_backtest_postprocess.py tests/test_lol_backtest.py
make lint-all
git commit -m "$(cat <<'EOF'
refactor: drop forced-hold accounting because no LoL map reaches the safety cap

EOF
)"
```

If a hook rewrites a staged file, fix, `git add` that file again, re-run `make lint-all`.

### Task 3 — docs + removal proof

#### T3.1 — Rewrite workflow facts

Apply §2.3.

#### T3.2 — Dead vocabulary gone from active code/contracts

```bash
rg -n 'forced_hold|forced-hold|cutoff_qty_[01]|cutoff_mid_[01]|pnl_at_cutoff|tail_pnl|settled_pnl|still_going|cutoff_rule|LOL_MARKET_END_SECOND|REASON_MISSING_CUTOFF_MIDPOINT' src tests scripts AGENTS.md docs/domain.md
```

Expected: **no matches**. Historical experiment documents are outside this check. `buy_cutoff_ns` / `gate_cutoff_seconds` / `synthetic_cutoff_pauses` must **not** match (they do not today against this pattern).

If this `rg` still hits `docs/superpowers/plans/2026-09-03-remove-lol-forced-hold.md`, you scoped wrong — that file is not in the path list.

#### T3.3 — Ordinary map-end still exists

```bash
rg -n 'game_end_ns|_on_game_end|last_frame_wall_seconds' src/backtest src/lol tests/test_backtest_maker.py tests/test_lol_prepare_backtest.py
```

Expected: matches in replay bounds, strategy setup, game-end callback/cancellation tests, and final-frame dataset tests.

#### T3.4 — Final verification

```bash
uv run pytest tests/test_lol_prepare_backtest.py tests/test_lol_prepare_dataset.py tests/test_lol_backtest.py tests/test_backtest.py tests/test_backtest_validation.py tests/test_backtest_postprocess.py tests/test_backtest_maker.py -q
git add AGENTS.md docs/domain.md docs/superpowers/plans/2026-09-03-remove-lol-forced-hold.md
make lint-all
```

Stage **only** those three paths for this commit. If `git status` shows `.streamlit/config.toml`, `docs/next_steps.md`, or the collapse plan, you staged too much — unstage them.

Untracked source plan is skipped by lint until added: `git add` then re-run `make lint-all`.

```bash
git commit -m "$(cat <<'EOF'
docs: record final-frame settlement after zero forced-hold maps

EOF
)"
git status
```

Confirm three new commits (or one squash), unrelated dirt still unstaged, collapse plan still untracked.

---

## 4. Workspace progress (after the product commits)

Append to `/Users/dimabytes/work/polymarket/dota_2_bot/betting_workspace/current-task/progress.txt`. Never replace. Skill format:

```
## [Date/Time] - US-002

- What was implemented
- **Learnings for future iterations:**
  - Patterns discovered
  - Gotchas encountered
  - Useful context
```

Keep it short. Useful notes if true: `LOL_PREPARE_END_SECOND = LOL_FETCH_END_SECOND` (do not repeat 7200); checkpoint resume is column selection not fill-defaults; `buy_cutoff_ns` stayed; collapse plan unstaged; three product commits and their SHAs.

Then commit in **betting_workspace** only this plan and progress:

```bash
cd /Users/dimabytes/work/polymarket/dota_2_bot/betting_workspace
git add current-task/plans/US-002.md current-task/progress.txt
git commit -m "$(cat <<'EOF'
docs: US-002 LoL forced-hold removal progress

EOF
)"
```

Do **not** add `current-task/feature.json`. `passes` stays `false`.

---

## 5. Do not touch

- `../poly-maker`
- `.streamlit/config.toml`, `docs/next_steps.md`
- `docs/superpowers/plans/2026-09-03-collapse-canonical-pipeline-optionality.md`
- `docs/experiments/**`, `docs/no-unwind-default-plan.md`, `docs/live-paper.md`
- `src/backtest/strategy.py` (`game_end_ns`, `_on_game_end`, `buy_cutoff_ns`)
- `tests/test_backtest_maker.py` (must remain byte-stable except if a constructor elsewhere forces a change — it should not)
- `LOL_FETCH_END_SECOND`, `LOL_FETCH_MAX_WALL_SECONDS`, train/label `0..540`
- `onchain_fills` / `trades` selection
- US-001 launcher text; US-003–US-009 collapse work
- `feature.json` `passes`

---

## 6. Risks

| Risk | What to do |
|---|---|
| Deleting `buy_cutoff_ns` / `GateSeconds.cutoff` / `synthetic_cutoff_pauses` because the name says cutoff | Leave them. Forced-hold only. |
| Keeping `_fill_cutoff_checkpoint_columns` “for old parquets” | Forbidden. Select current fields; extras ignored. |
| `local`-style extra compatibility branch in `read_results_checkpoint` | Copy the spec function. |
| Staging the collapse plan or `next_steps.md` / streamlit | `git status` before add. Task 3 adds the forced-hold plan only. |
| Redoing US-001 four-shard sentences while in `AGENTS.md` / `domain.md` | Leave L31 / L128–131 / examples. |
| Running a real backtest to “confirm 0 forced-hold” | Out of scope. Unit tests + `rg`. |
| Editing `docs/experiments/lol-no-unwind.md` because it says `forced_hold` 0 | Historical. Leave it. |
| Forgetting `BACKTEST_AUDIT_INTEGER_COLUMNS` `"cutoff_ts"` | TypedDict drop + leftover integer list = write error. Delete L152. |
| One-commit squash leaving transitional `forced_hold=False` | If one commit, fields are already gone — do not pass deleted kwargs. |
| Setting `passes: true` | Orchestrator after review. |
| `make lint-all` before adding the untracked source plan (Task 3) | Hook skips it; add then re-run. |

---

## 7. Acceptance checklist (from `feature.json` US-002 `changes[]`)

Tick against the diff, not memory.

- [ ] Entire source plan `2026-09-03-remove-lol-forced-hold.md` implemented (Tasks 1–3)
- [ ] `ReplayBounds` / audit schema drop `still_going` / `cutoff_ts`; `MarketContext` drops `forced_hold` / `cutoff_ns`
- [ ] `MakerMatchResult`, summaries, wallet settlement, and reports use `engine_pnl` and `game_ended_at` only
- [ ] `read_results_checkpoint` ignores extra legacy columns by selecting current fields (no fill branch)
- [ ] `LOL_PREPARE_END_SECOND = LOL_FETCH_END_SECOND`; `LOL_MARKET_END_SECOND` and `REASON_MISSING_CUTOFF_MIDPOINT` gone
- [ ] `game_end_ns` / `_on_game_end` kept; train rows 0..540; onchain_fills vs trades unchanged
- [ ] `AGENTS.md` and `docs/domain.md` updated; historical experiment docs unchanged
- [ ] Tests updated: schema asserts, `test_context_ends_at_final_livestats_frame`, `test_read_results_checkpoint_ignores_legacy_cutoff_columns`; forced-hold tests deleted
- [ ] Task 3 `rg` (dead vocab) is empty; map-end `rg` still hits
- [ ] Focused pytest suites pass; `test_backtest_maker.py` still passes unchanged
- [ ] `make lint-all` passes after staging (including the source plan on Task 3)
- [ ] Typecheck: basedpyright via `make lint-all`
- [ ] Product commits in `dota_2_model` with the source-plan subjects (prefer three)
- [ ] Forced-hold plan staged with Task 3; collapse plan / streamlit / `next_steps.md` still unstaged
- [ ] Progress appended in skill format
- [ ] Workspace commit of this plan + `progress.txt` only
- [ ] `feature.json` `passes` still `false`
- [ ] US-003 not started
