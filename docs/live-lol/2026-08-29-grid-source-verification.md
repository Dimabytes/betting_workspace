# Проверка GRID как live-источника LoL

Дата: 2026-08-29

Проверяем три пункта перед реализацией плана
`2026-08-29-live-lol-implementation-plan.md`:

1. совпадают ли нули часов GRID и lolesports livestats;
2. сходятся ли 12 фич модели между источниками;
3. сколько LoL-объёма на Polymarket несёт `gridSeriesId`.

Все команды запускаются из `../dota_2_model`.

## Что уже проверено 2026-08-29

### Пункт 3 закрыт

```
make run F=scripts/probe_lol_grid_coverage.py ARGS="--days 21"
```

Результат за 21 день:

| Метрика | Всего | С `gridSeriesId` | Доля |
|---|---|---|---|
| События | 321 | 194 | 60.4% |
| Map-рынки | 792 | 484 | 61.1% |
| Объём map-рынков | 145.3M | 106.3M | **73.1%** |

Пропускаем в основном LPL (tes, lgd, edg, nip, tt, ig, blg, al, we) и часть
LCK. Оценка из `RESEARCH.md` «примерно половина» верна по числу карт. По
объёму мы держим три четверти.

### Парсер GRID разбирает LoL без изменений

Запись живого LEC-матча `lol-navi-gx-2026-08-29`, серия `2966907`:

- `read_net_worth` вернул 10 игроков и два team id без правок;
- строка игрока несёт `NetWorth`, `Kills`, `Deaths`, `KillAssistsGiven`,
  `increaseLevel`, а также `ExperiencePoints`;
- уровни на конце карты 13–18, значения правдоподобные;
- стороны в scoreboard подписаны `BLUE` и `RED`;
- `series_table` объявил `delay = 8`;
- `hero` пустой: портрет LoL это uuid чемпиона. На фичи не влияет.

Вывод: для LoL меняем только имена сторон и таблицу XP. Больше в парсере
ничего.

### Livestats действительно отстаёт

Прямая проба окна для живой карты:

| Отступ от wall clock | Ответ |
|---|---|
| 30 с | 400 |
| 45 с | 400 |
| 60 с и больше | 204 или 200 |

Окно моложе 60 секунд feed не отдаёт. Это подтверждает замер из
`dota_2_model/docs/experiments/lol-grid-widget.md`. Рекордер по умолчанию
берёт отступ 75 секунд.

## Результаты пунктов 1 и 2

Записаны два матча 2026-08-29 по 12 минут:

| Матч | GRID series | esportsGameId | Пересечение |
|---|---|---|---|
| CBLOL `lol-fxw7-los-2026-08-29` | 2973268 | 115565671526402982 | ~700 с игры, 10 смертей |
| LEC `lol-navi-gx-2026-08-29` | 2966907 | 115548681803406157 | ~70 с игры, 0 смертей |

Основной вывод из CBLOL. LEC служит перекрёстной проверкой пауз.

Сырые записи закоммичены в `recordings/`. Как перепроверить — в
`recordings/README.md`. Вывод компаратора на момент замера — в
`recordings/comparator-output.txt`.

### Пункт 1: часы совпадают. Проблемы нет

`grid_second = livestats_second + 6.7`, разброс 1.7 секунды на 10 смертях
за 700 секунд игры.

| Событие | GRID | livestats | Разница |
|---|---|---|---|
| blue #1 | 377.0 | 369.6 | +7.4 |
| blue #2 | 378.0 | 371.5 | +6.5 |
| blue #3 | 386.0 | 379.2 | +6.8 |
| blue #4 | 508.0 | 501.2 | +6.8 |
| blue #5 | 509.0 | 503.3 | +5.7 |
| blue #6 | 600.0 | 592.9 | +7.1 |
| red #1 | 381.0 | 375.0 | +6.0 |
| red #2 | 409.0 | 403.1 | +5.9 |
| red #3 | 662.0 | 655.4 | +6.6 |
| red #4 | 701.0 | 694.2 | +6.8 |

7 секунд меньше, чем `source_lag_seconds = 10` в `model.json`. Правка не
нужна.

**Первый замер дал −25 секунд и был неверным.** Компаратор считал время
livestats как `wall − spawn_wall` и не вычитал паузы, а на карте CBLOL была
пауза 32 секунды. Пайплайн обучения вычитает паузы через `assign_game_times`.
Компаратор теперь использует ту же функцию.

Перекрёстная проверка пауз. На карте LEC:

- livestats насчитал одну паузу 99.5 секунды;
- якорь часов GRID `occurred_at − clock_seconds` прыгнул ровно на 100 секунд.

Два независимых источника согласились. На CBLOL якорь GRID не двигался вовсе:
разброс 0 на 165 фреймах.

Поиск сдвига по кривой золота ненадёжен: дно плоское, и он даёт +22 вместо
+7. Причина в смещении net worth ниже. Меряем по смертям.

### Пункт 2: одна настоящая проблема

**GRID `NetWorth` и livestats `totalGold` — разные величины.**

Из сырого GRID-фрейма видно: `NetWorth = Money + LoadoutValue`. Проверено на
игроках: 365 + 1700 = 2065, 1381 + 2150 = 3531. Это текущее нерастраченное
золото плюс стоимость предметов.

livestats `totalGold` — всё заработанное с начала карты. Разница это варды,
зелья и потери на перепродаже.

Отношение GRID к livestats на 1566 парах при сдвиге +7:

| Сторона | p10 | медиана | p90 |
|---|---|---|---|
| blue | 0.947 | **0.957** | 0.976 |
| red | 0.925 | **0.949** | 0.958 |

GRID ниже на 4–5%, и стороны смещены по-разному. Разница между сторонами и
даёт смещение главной фичи:

| Величина | p10 | медиана | p90 |
|---|---|---|---|
| `nw_adv` GRID минус livestats | −15 | **+133** | +382 |
| `nw_adv` сам сигнал livestats | −1033 | −145 | +446 |

**Знак `nw_adv` расходится на 15% сэмплов (242 из 1566).**

`xp_adv` на правильном сдвиге ведёт себя лучше: медиана расхождения 0,
p10/p90 равны −480 и +480 при собственном размахе сигнала −980..+1280.
Знак расходится на 13%. Ранее я приводил для `xp_adv` расхождение 680 —
это была цифра на неверном сдвиге +22.

Остальные фичи:

| Фича | Расхождение |
|---|---|
| `deaths_blue`, `deaths_red` | совпадают точно |
| `top1_ratio_over_rest` | 1.8% |
| `top1_ratio_over_total` | 1.3% |
| `xp_adv` | медиана 0, но p10/p90 равны −480/+480, знак расходится на 13% |

### Побочные находки

**Формулы top1 расходятся.** Модель LoL обучена на `top1 / сумма`
(`lol/livestats_frames.py:397`). Live-путь Dota считает `top1 / (сумма − top1)`
(`shared/utils/top_players.py`). На спавне 0.20 против 0.25. Это баг в плане,
а не в данных.

**GRID не шлёт `increaseLevel`, пока на карте не случился первый level-up.**
`_player_from_row` тогда ставит всем уровень 1. Это верно до первого
level-up, но `xp_adv` в ранней игре шумит. У GRID есть сырой
`ExperiencePoints` на игрока, который точнее лестницы уровней. Модель обучена
на лестнице.

**Разрежённость GRID.** На CBLOL 167 сэмплов за 715 секунд, то есть один раз
в 4.3 секунды. livestats даёт около 3 Гц. Часть разброса выше это разрежённость,
но медианное смещение +133 ею не объясняется: симметричный шум дал бы медиану
около нуля.

### Решение: GRID-золото восстанавливается из истории lolesports

GRID подтверждает расхождение [официально в FAQ](https://grid.helpjuice.com/client-help/why-is-networth-on-grids-api-different-from-the-net-worth-total-gold-value-on-the-broadcast-lol):
их `netWorth = money + loadoutValue`, Riot `totalGold = money + loadoutValue +
стоимость потреблённых предметов`. «Работаем над исправлением, ETA нет».
Значит:

```
nw_rec(t) = totalGold(t) − consumed(t)
```

`consumed` считается по эндпоинту `details` (мы его не собирали — только
`window`): он работает и для прошедших игр, отдаёт те же кадры с `items`
(инвентарь) и `totalGoldEarned`, равным `totalGold` окна. Падение количества
предмета с флагом `consumed` из Data Dragon между соседними кадрами × его
цена — и есть потреблённое золото.

Проверено на записи CBLOL против настоящего GRID NetWorth, сдвиг +7, 1235 пар
(`scripts/reconstruct_lol_networth.py`):

| Величина | сырое totalGold | реконструкция |
|---|---|---|
| blue: медиана ratio к GRID | 1.046 | **1.002** |
| red: медиана ratio к GRID | 1.054 | **0.997** |
| blue/red: медиана \|x − GRID\| | 545 / 715 | **44 / 69** |
| nw_adv: медиана gap | −135 | **+17** |
| nw_adv: перевороты знака | 16.0% | **5.8%** |

Систематики по времени в остатке нет (медианы 2-минутных бакетов ±100).
Оставшиеся 5.8% переворотов сидят около нулевого преимущества — это шум
разрежённости GRID, не смещение. Побочно объяснена асимметрия сторон: red
потратил на расходники 1275 золота против 850 у blue, разница 425 — тот же
порядок, что asymmetry сырых гэпов (715 против 545).

Оговорки реконструкции:

- Одна карта. Прогонять `reconstruct_lol_networth.py` на каждой новой двойной
  записи — скрипт сам достаёт и кэширует `details` и таблицу цен.
- Цены взяты из последнего патча Data Dragon. Для рефетча истории брать патч
  из `gameMetadata.patchVersion`, который уже лежит в каждом window-архиве.
- Продажа предметов неотличима от потребления; в первые 10 минут про-игры её
  почти нет. Эликсиры — после 20-й минуты, вне окна обучения 0..540 с.
- Точного равенства с GRID не будет никогда: карманное золото lolesports не
  отдаёт. Но остаток 0.2–0.5% без систематики — на порядок меньше сырого
  расхождения и без смещения знака.

### Что это значит для плана

Пункт 1 закрыт, правок не требует. Пункт 5 закрыт. Для net worth есть
рабочий путь.

Варианты:

| Вариант | Оценка |
|---|---|
| Проверить чувствительность на бэктесте: прогнать LoL-валидацию с искажением золота по замеренной форме (дрейф 0.97→0.95 плюс асимметрия сторон) и посмотреть дельту PnL | Дёшево, отвечает «нужна ли правка вообще». **Делаем это первым** |
| Переобучить на реконструированном NetWorth: дофетчить `details` для связанных карт (4992 архива × ~122 окна ≈ 645k запросов, 128 потоков — порядка часа, если фид не начнёт отдавать 429; `details` отвечает 200 даже для карт октября 2025), фичи `totalGold − consumed` | **Рабочий путь, если бэктест покажет чувствительность.** Реконструкция сходится с GRID до 0.2–0.5% |
| Переобучить на записях GRID | Не нужно и невозможно: исторический GRID отдаёт только конечный снапшот серии, прогрессии золота нет |
| Торговать по livestats | Отпадает: 60 секунд отставания |

Записи закоммичены в `recordings/`. Компаратор читает `.jsonl` и
`.jsonl.gz`, поэтому измерение воспроизводится без живого матча.

## Как повторить: пункты 1 и 2


Нужна одна живая карта, записанная с обоих источников одновременно.

### Шаг 1. Найти живую карту

```
make run F=scripts/record_lol_dual_feed.py
```

Скрипт печатает два списка: live-события Polymarket с `gridSeriesId` и
live-игры lolesports. Нужна карта, которая есть в обоих.

### Шаг 2. Записать

```
make run F=scripts/record_lol_dual_feed.py ARGS="<slug>"
```

Пример: `ARGS="lol-navi-gx-2026-08-29"`.

Скрипт сам находит `gridSeriesId` по slug и `esportsGameId` по кодам команд.
Если совпадений несколько, добавь `--esports-game-id <id>`.

Пишет в `data/lol_dual_feed/<slug>-<stamp>/`:

- `grid.jsonl` — сырые фреймы widget socket с меткой получения;
- `livestats.jsonl` — сырые ответы окна с меткой получения;
- `target.json` — что именно записано.

Останавливать Ctrl-C. **Нужно минимум 8 минут игрового времени.** Меньше — и
поиск сдвига не наберёт пар.

Записывать надо карту, которая только начинается. Если карта уже идёт
десятую минуту, livestats всё равно догонит по окнам, а GRID начнёт с
текущего состояния. Для поиска сдвига этого хватит, но чем длиннее общий
отрезок, тем надёжнее.

### Шаг 3. Сравнить

```
make run F=scripts/compare_lol_grid_livestats.py ARGS="data/lol_dual_feed/<run>"
```

Скрипт делает три вещи:

1. Приводит фреймы GRID к тем же 12 фичам, что читает модель. Секунда
   считается как в `grid_feed.py`: часы scoreboard плюс возраст метки минус
   `feed_delay` таблицы.
2. Приводит окна livestats к фичам через готовый `features_from_sides`.
   Ноль часов — первый фрейм с ненулевым золотом, как в обучении.
3. Ищет сдвиг от −120 до +120 секунд, который лучше всего совмещает кривые
   золота синей стороны. Затем печатает расхождение по каждой фиче.

## Как читать результат

### Сдвиг часов

```
best offset: grid_second = livestats_second +N
```

- `N` близко к нулю: часы совпадают, правка не нужна.
- `N` постоянный и заметный: добавляем константу в GRID-путь LoL.
- Соседние сдвиги дают почти такую же ошибку: кривая пологая, одного матча
  мало. Записываем второй матч.

Ниже печатаются сдвиги в окне ±3 секунды. Если их стоимость близка, оценка
неточная.

### Расхождение фич

Для каждой фичи выводится медиана `|grid − livestats|` и та же величина в
процентах от livestats.

Ожидания:

- `deaths_blue`, `deaths_red` — должны совпадать точно. Расхождение значит,
  что сдвиг подобран неверно.
- `blue_nw`, `red_nw` — livestats берёт `totalGold` команды, GRID суммирует
  `NetWorth` игроков. Небольшая разница нормальна. Больше 3% — разбираемся.
- `xp_adv` — обе стороны считаются по `LOL_LEVEL_XP` от уровня. Должны
  совпадать почти точно.

### Отдельная проверка: две формулы top1

Скрипт печатает обе:

- `top1_ratio_over_rest` — `top1 / (сумма − top1)`, правило
  `shared/utils/top_players.py`, которое использует live-путь Dota;
- `top1_ratio_over_total` — `top1 / сумма`, правило
  `lol/livestats_frames.py:features_from_sides`, на котором обучена модель
  LoL.

**Это уже найденное расхождение, а не гипотеза.** Модель LoL обучена на
`top1 / сумма`. Если в LoL-ветке live-пути переиспользовать
`build_top_player_features`, две из двенадцати фич поедут.

Правка в стадию 5 плана: `build_top_player_features` получает правило игры,
или LoL-ветка считает ratio своей функцией. Решаем после того, как увидим
цифры.

## Что делаем с результатом

| Результат | Действие |
|---|---|
| Сдвиг около нуля, фичи сходятся | План идёт как написан, плюс правка top1 ratio |
| Сдвиг постоянный | Добавляем константу в GRID-путь LoL, план идёт дальше |
| Сдвиг плавает по матчу | GRID нужен свой якорь нуля. Стадия 5 растёт |
| Золото расходится больше 3% | Случилось. Решение — реконструкция `totalGold − consumed`, см. выше. На новой записи прогнать `reconstruct_lol_networth.py` и убедиться, что ratio остаётся около 1.00 |

Записанный `grid.jsonl` в любом случае идёт в
`tests/lol_grid_widget_fixtures.py` как фикстура стадии 0.
